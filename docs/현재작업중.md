# 현재 작업중

## 2026-07-27
- `articles/gen-z-last-generation.html`: 공개 글의 상단 고지, 출처 목록, 구조화 데이터에서 원고 제공 경로에 관한 표기를 제거했다. 통계 자료를 별도로 대조했다는 사실과 인과관계 해석이 필자의 관점이라는 편집 고지는 유지했다.
- `articles/index.html`, `style.css`: 연마의 새 아티클 허브를 추가했다. `전체·에세이·시황분석·건강정보` 카테고리를 URL query로 구분하고, 정적 HTML 카드와 발행일 기준 정렬 스크립트를 함께 사용해 JavaScript가 없어도 글이 노출되면서 실행 시에는 항상 최신 글이 먼저 오도록 구성했다. 카테고리 링크는 현재 상태와 글 수를 표시하며, 글이 없는 카테고리에는 빈 상태 안내를 제공한다.
- `articles/gen-z-last-generation.html`: 사용자가 제공한 원고의 55개 본문 단락 중 통계 문단을 제외한 54개를 그대로 보존하고, 통계 문단은 출처를 확인할 수 있는 표현으로 다듬어 에세이 「인류의 마지막 세대가 될지도 모르는 Z세대」로 편집했다. 장문 전용 72자 폭, 목차, 카테고리·발행일·읽기시간·의견 글 표시, 편집 범위 공개, 자료·통계 출처와 조사 한계를 포함했다.
- `index.html`, `app.html`, `i18n.js`, `style.css`: 홈과 실행 앱 헤더에서 `가이드` 왼쪽에 `아티클`을 추가하고 푸터와 한영 번역에도 연결했다. 링크 증가로 중간 화면 폭에서 중앙 제목과 내비게이션이 겹치지 않도록 721~1040px 헤더를 두 줄 그리드로 전환하고, 모든 헤더 링크와 언어 선택기의 최소 높이를 44px로 맞췄다. 스타일·번역 캐시 버전을 `20260727-1`로 갱신했다.
- `about.html`, `contact.html`, `privacy.html`, `terms.html`, `affiliate-disclosure.html`, `guides/*.html`: 지원 페이지와 모든 독립 가이드의 상단 이동·푸터에서 아티클 허브로 왕복할 수 있도록 내부 링크를 통일했다.
- `amplify.yml`, `sitemap.xml`: Amplify 배포 허용 목록에 `articles/*.html`을 추가하고 사이트맵에 아티클 허브와 첫 글 canonical URL을 등록했다. 루트 수정일은 `2026-07-27`로 갱신했다.
- 검증: 18개 HTML의 내부 링크·로컬 자산·중복 ID·JSON-LD 구문, 아티클 필터 인라인 JavaScript와 `i18n.js` 구문, 사이트맵 15개 URL의 실제 파일·canonical·index 가능 여부, `git diff --check`를 검사했다. 공유 원고와 게시 본문을 단락별로 대조해 통계 출처를 명확히 고친 한 문단 외 누락이 없음을 확인했다.

## 2026-07-26
- `index.html`, `app.html`, `style.css`: AdSense의 `가치가 별로 없는 콘텐츠` 재거절을 기준으로 기존 6개 도구 단일 SPA를 콘텐츠 중심 홈과 실행 전용 앱으로 분리했다. 루트는 클라이밍 측정·호흡 훈련을 주제로 도구 목적, 대상, 한계, 검증 원칙과 최근 변경 기록을 서버 HTML 본문으로 제공하고, 기존 `?tab=` 공유 주소는 `app.html?tab=`으로 이어지게 했다. 실행 앱은 조작 중심 화면이 검색 결과의 대표 콘텐츠가 되지 않도록 `noindex,follow`와 독립 canonical을 적용했다. 실서비스 데스크톱 점검에서 대형 제목의 끝 두 글자가 별도 줄로 떨어지는 조판을 확인해 제목 문구를 간결하게 다듬었다.
- `guides/sequence-generator.html`, `guides/lottery-generator.html`, `guides/index.html`: 비디오 시퀀스 제작과 로또 6/45 조합 생성기의 독립 가이드를 새로 만들고, 실제 처리 순서·입력 예시·알고리즘·실패 조건·브라우저 한계·개인정보 처리·확률 한계를 공개했다. 가이드 허브를 6개 도구와 일치시키고 각 글로 직접 이동하도록 내부 링크와 `CollectionPage` 구조화 데이터를 갱신했다.
- `guides/abrahang.html`, `guides/breathing.html`, `guides/co2-table.html`, `guides/foot-analysis.html`, `about.html`: 기존 가이드에 보이는 작성·기능 검증 주체, 최종 검증일, 실제 입력→결과 예시, 앱 설계값과 외부 근거의 구분, 알려진 측정 한계를 보강하고 `Article` 구조화 데이터를 통일했다. 소개 페이지에는 운영 주체, Who/How/Why, 테스트·문헌 확인 방식, 자동화 사용 범위, 정정 정책과 최근 변경 기록을 사실 범위 안에서 공개했다.
- `i18n.js`, `app.html`: `연마`·`HPMPLab`·`Grind.app`·`Timer.app`으로 나뉘던 브랜드를 HPMPLab의 `연마`로 통일했다. 로또 화면의 `추천 번호`·`구간 디퓨전` 표현은 예측 성능으로 오해하지 않도록 `생성 번호 세트`·`구간 분포 반복 조정`으로 바꾸고, 모든 유효 조합의 당첨 확률이 같다는 안내를 상시 표시한다. 외부 Tistory 핵심 링크와 AliExpress 원격 이미지를 제거해 내부 원본 콘텐츠와 로컬 자산을 우선하게 했다.
- `contact.html`, `privacy.html`, `terms.html`, `affiliate-disclosure.html`, `sitemap.xml`: 문의·정정 요청 범위, Formspree 처리 주체와 삭제 요청, 로컬 파일·측정 데이터 처리, 의료·예측·변환 결과의 한계, 제휴 고지를 구체화했다. 사이트맵은 색인 가능한 콘텐츠 13개와 실제 수정일을 반영하고 실행 앱은 제외했다.
- 검증: 16개 HTML의 내부 링크·로컬 자산·중복 ID·JSON-LD 구문, 사이트맵 13개 URL의 파일·canonical·index 가능 여부, JavaScript 구문, `git diff --check`를 검사했다. 콘텐츠 본문은 홈 535단어, 소개 568단어, 가이드 허브 368단어, 독립 가이드 562~1,041단어로 확인했으며 `Grind.app`·`Timer.app`·외부 Tistory·예측성 추천 문구 잔재가 없는지 검색했다.

## 2026-07-15
- `style.css`, 공통 HTML: 아브라행 링 게이지 상단 좌우의 `현재 하중`·`피크 하중` 라벨과 실시간 측정값 글자를 화면 폭에 따라 더 크게 보이도록 반응형 폰트로 조정하고, 스타일 캐시 버전을 `20260715-3`으로 갱신했다.
- 검증: Chrome Playwright 320·360·600·1024·1440px에서 라벨은 약 12.2~13.4px, 측정값은 약 16.6~19.2px로 확대되는지 확인했다. 긴 추정값 `~110.0kg`도 카드 내부와 문서 폭을 넘지 않았고 중앙 타이머와 겹치지 않았다.
- `style.css`, `index.html`, `about.html`, `privacy.html`, `terms.html`, `contact.html`, `affiliate-disclosure.html`, `guides/*.html`: 모바일에서만 보이던 아브라행 원형 게이지 상단의 `현재 하중`·`피크 하중` 좌우 카드를 데스크톱 브라우저에서도 같은 위치와 크기 규칙으로 항상 표시하도록 반응형 조건을 공통 스타일로 옮기고, 스타일 캐시 버전을 `20260715-2`로 갱신했다.
- 검증: Chrome Playwright에서 360·600·768·1024·1440px 화면의 두 하중 카드가 모두 `display: flex`로 노출되고 원형 게이지 상단 좌우에 대칭 배치되는지 확인했다. 중앙 시간 표시와 충돌, 문서 가로 넘침, 페이지 런타임 오류는 없었다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 무수신 판정 90초보다 여유를 두기 위해 맥스행 시도 사이 휴식을 60~75초(15초 단위), 기본 75초로 줄이고 수행 순서를 `왼손1 → 오른손1 → 왼손2 → 오른손2 → 왼손3 → 오른손3`으로 바꿨다. 저울에는 약 75초마다 다시 하중이 걸리며, 기본값에서 같은 손은 반대손 5초 측정과 두 휴식을 포함해 약 155초를 회복한다. 실행 순서가 바뀌어도 결과 카드와 저장 기록은 기존처럼 왼손 1~3·오른손 1~3 순서로 보이도록 프로토콜 인덱스를 별도로 매핑하고, 과거 120~300초 히스토리의 실제 휴식값은 그대로 보존한다.
- `abrahang-timer.js`, `index.html`, `i18n.js`: 각 시도에서 실제 BLE 수신 시각을 이용한 최고 1초 시간가중 이동평균 힘, 힘 상승 시작 후 200ms 시점 힘, 같은 손의 세 시도 중 최고 1초 힘의 90% 이상을 연속 유지한 최장시간을 계산한다. 손별 `3회 출력 감소율`은 `1 - (세 시도 합 / (3 × 최고 시도))`으로 최고 시도 대비 평균 결손률을 표시한다. 1초/200ms/90% 지표는 샘플 밀도와 최대 공백으로 `유효/추정/샘플 부족`을 구분하고, 추정값에는 `~`를 붙이며 순간 피크를 1초 값으로 대체하지 않는다. 저울은 자세 자체를 판별하지 못한다는 한계를 결과 설명에 명시했다.
- `abrahang-timer.js`: 맥스행 첫 시도만 독립 3초 프리카운트를 사용하고, 두 번째 시도부터는 휴식 마지막 3초를 다음 시도의 기준선 수집과 준비 카운트로 함께 사용한 뒤 곧바로 측정한다. 이로써 휴식 끝의 `3초 + 별도 3초` 중복을 제거했다. 마지막 3초 중 일시정지해도 다음 시도의 기준선 샘플 시각을 정지 시간만큼 보정하며, WH-C06 페이지 복구 payload를 교대 순서와 호환되는 v4로 갱신하고 구형 맥스행 진행 중 복구 데이터는 안전하게 무시한다.
- `abrahang-timer.js`: Abrahangs/Emil의 그립·형태·깊이·한손 수행 손 변경과 맥스행의 손 전환을 실제 프로토콜 단계로 비교한다. 변경이 있으면 기존 3초 카운트의 첫 560Hz 짧은 음만 960Hz·0.72초 주의 알람으로 대체하고, 2초·1초 기본음과 시작음은 유지한다. 로딩 종료 시에는 일반 전환음만 재생해 알람이 일찍 또는 추가로 울리지 않으며, Video의 같은 중간 2손가락 안에서 오픈→하프·깊이 변경도 감지한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 좌우 결과 카드와 히스토리 항목에 날짜·그립을 포함한 접근성 이름을 연결하고, 삭제 버튼도 여러 기록의 대상을 구분할 수 있게 했다. 420px 이하에서는 손별 지표를 세로 목록, 각 시도를 가로 지표 행으로 전환하고, 히스토리는 모바일 2열·넓은 화면 4열로 정렬했다.
- `guides/abrahang.html`, `index.html`, `i18n.js`: 좌우 교대 순서, 75초 기본 휴식과 같은 손 약 155초 회복 구조, 휴식 마지막 3초에 포함되는 단일 프리카운트, 변경 시 첫 음만 길어지는 규칙을 한영 안내와 가이드에 반영했다. 좌우 교대 5초 측정 연구 링크를 추가하고 루트의 i18n/아브라행 스크립트 캐시 버전을 `20260715-2`로 갱신했다.
- 검증: JavaScript 구문과 diff를 검사했다. Chrome Playwright 수동 RAF와 합성 BLE 패킷으로 기본값 75초·전체 6분 48초, `L1/R1/L2/R2/L3/R3` 실행과 다음 손 결과 카드 강조·히스토리 재정렬, 과거 휴식 180초 기록 보존을 확인했다. 다섯 전환 모두 `960Hz 0.72초 → 560Hz 2·1초 → 740Hz 시작음` 한 번씩만 재생되고 별도 프리카운트 없이 휴식에서 바로 측정으로 넘어갔다. 양손의 같은 설정 반복은 기본 짧은 음, 한손 모드의 손 교대와 Paper 그립 변경, Video 중간 2손가락 오픈→하프·깊이 변경은 긴 음으로 구분됐다. 손별 1초 피크 110kg, 200ms 힘 31.5kg, 90% 유지 2.2초, 출력 감소율 10.0%, 유효/추정/샘플 부족, 측정 중 10초 일시정지, 한영 전환, 320/360/1440px 가로 넘침 0과 모드 버튼 44px 이상도 재확인했다.

## 2026-07-14
- `index.html`, `style.css`, `i18n.js`: 기능·디자인·접근성 감사를 반영했다. 앱 탭을 WAI-ARIA 탭 버튼으로 바꾸고 좌우 화살표/Home/End 키 이동, 패널 `hidden` 동기화, 본문 바로가기, 고대비 포커스 링, 폼 접근 가능한 이름, 단계 전환에만 알리는 상태 영역을 추가했다. 모바일 헤더와 탭을 압축하고 320px에서 호흡·발·시퀀스·아브라행 가로 넘침을 제거했으며, 공유 버튼 터치 영역 44px와 `prefers-reduced-motion` 대응을 적용했다. 호흡 화면의 좌측 주파수/중앙 원형 게이지/우측 파란 세로 볼륨 구조는 유지했다.
- `index.html`, `main.js`, `foot-analysis.js`, `sequence-generator.js`, `lottery-generator.js`: 기본 호흡 탭에서 사용하지 않는 CO2·발 핏·시퀀스·로또·아브라행 스크립트를 각 탭 첫 진입 때만 불러오도록 바꿨다. 로또 42만 바이트 이력 JSON도 로또 첫 진입 또는 생성/갱신 때만 한 번 로드하고 버전 URL의 브라우저 캐시를 재사용한다. 지연 로딩 중인 도구 패널은 `aria-busy`와 `inert`로 잠그고 스크립트 실행 직후 탭 활성 이벤트를 다시 보내, 매우 빠른 첫 조작이 초기화 전에 유실되지 않게 했다. 시퀀스의 런타임 오류·파일 정보·프리셋 상태 문구를 한글/영문으로 통일하고, 모션 감소 설정에서는 강제 부드러운 스크롤과 호흡 버블 변형을 멈춘다.
- `index.html`, `foot-analysis.js`, `i18n.js`, `guides/foot-analysis.html`, `guides/index.html`, `about.html`, `privacy.html`: 기존 발 사진 기능이 실제 픽셀 측정을 하지 않는데도 사진 기반 치수·형태 분석처럼 보이던 문제를 제거했다. 사진은 선택적 비교 기록으로 분리하고, 좌우 실측 길이와 발볼·발등·아치·발가락 자가 체크를 그대로 표시하는 암벽화 핏 체크리스트로 교체했다. 가짜 seed/너비 mm/브랜드 모델 점수/사이즈 환산표와 과거 가짜 계산 이력의 노출을 삭제하고, 손상된 로컬 이력을 안전하게 처리하며 실제 착화와 제조사 공식표를 우선하도록 한계를 명시했다.
- `index.html`, `i18n.js`, `guides/breathing.html`, `guides/co2-table.html`: 루트 페이지의 과도한 효과 표현과 숨참기 처방처럼 보이는 문구를 정리했다. 호흡은 이완 보조 도구이며 의료 치료를 대신하지 않는다는 NCCIH 기준, CO2 테이블은 과호흡 금지·수중 단독 훈련 금지·버디 필요라는 DAN 안전 기준을 독립 가이드에 출처와 함께 추가했다. 루트의 대용량 인라인 가이드 복제를 제거하고 독립 가이드 링크로 바꿔 초기 DOM을 줄였다.
- `amplify.yml`, `customHttp.yml`, `404.html`: Amplify 산출물을 실제 서비스 파일과 헤더 설정 40개로 제한해 `ProjectInfo.MD`, `Blueprint.MD`, `Agent.MD`, `docs/**`, 훅/스크립트, 미사용 MP3와 원본 이미지를 공개 배포에서 제외했다. HSTS, SAMEORIGIN, nosniff, Referrer-Policy, 카메라만 허용하는 Permissions-Policy를 추가하고 외부 자산이 없는 반응형 404 페이지를 만들었다. Amplify 앱 규칙은 apex를 `www`로 보내는 301과 `/<*> -> /404.html`의 `404-200` rewrite로 정리해, 없는 파일과 내부 문서 요청이 원래 URL을 유지하면서 실제 HTTP 404와 새 오류 페이지를 반환하도록 적용했다.
- `.githooks/pre-commit`, `scripts/check_worklog_guard.sh`: 코드·설정 변경을 커밋할 때 `docs/현재작업중.md`가 함께 스테이징되지 않으면 커밋을 막는 저장소 로컬 가드를 추가하고 `core.hooksPath=.githooks`를 설정했다.
- 검증: 전체 JavaScript 구문과 diff, 13개 HTML의 로컬 링크/ID/ARIA 연결, Amplify 허용 목록을 검사했다. Chrome Playwright에서 320/360/1440 및 미디어쿼리 경계 21개 폭의 6개 탭 가로 넘침 0, 키보드 탭 전환, 한영 전환, 사진 없는 핏 체크리스트, 로또 5세트 생성, 시퀀스/아브라행 지연 로딩, 모션 감소 설정, 런타임 오류 0을 확인했다. Amplify main 배포 작업 317이 성공했고 실서비스에서 apex 301, 보안 헤더, 내부 문서/없는 파일의 HTTP 404 및 전용 404 본문을 재확인했다.

## 2026-07-08
- `index.html`, `abrahang-timer.js`: 아브라행 타이머에서 다음 로딩 세트의 그립이 직전 세트와 달라질 때 일반 전환음 대신 짧은 3음 패턴을 재생하도록 바꿨다. 같은 그립 반복 세트는 기존 전환음을 유지하고, 완료 알람의 긴 3회 비프는 그대로 유지한다.
- `main.js`, `index.html`: 호흡 타이머의 마지막 설정을 `localStorage`에 저장/복원하도록 추가했다. 들숨/날숨 시간, 동기화 여부, 자동 증가 on/off와 증가량, 차크라 선택, 볼륨을 저장하며 새로고침 후에도 마지막 설정값으로 시작한다. 진행 중 라운드/경과 시간은 저장하지 않고 설정만 유지한다.
- `index.html`, `style.css`, `i18n.js`: 모바일 첫 화면에서 기존 상단 네비게이션/앱 탭과 중복되어 보이던 `훈련 도구와 사용 가이드` 안내 박스를 제거했다. 상단 네비게이션의 `앱 가이드` 링크도 빼고 `가이드` 하나만 남겨 중복감을 줄였다. 루트 소개 문구와 별도 가이드 페이지는 유지하고, 가이드 접근은 상단 `가이드` 메뉴와 푸터/사이트맵으로 남겼다.
- `video-to-sprite-sheet.html`, `guides/*.html`: AdSense 심사 리스크를 줄이기 위해 사이트 구조에 편입되지 않은 `video-to-sprite-sheet.html`에 `noindex,nofollow`를 추가했다. 가이드 허브와 아브라행/호흡/CO2 테이블/발 분석 가이드는 루트 소개 문구를 늘리지 않고, 각 앱의 실제 사용 순서, 설정 예시, 기록 해석, 안전 기준, 한계 설명을 독립 페이지 본문에 보강했다. `https://hpmplab.com/`는 `https://www.hpmplab.com/`로 리다이렉트되는 것을 확인했으며 현재 HTTPS apex 응답은 302다.
- `index.html`, `style.css`, `i18n.js`: AdSense 콘텐츠 신호용 사이트 개요 블록이 첫 화면을 과도하게 차지하지 않도록 축소했다. 제목/설명 문구를 짧게 줄이고, 가이드 링크 4개는 큰 버튼 대신 작은 칩 링크로 표시하며 모바일에서는 세로로 자연스럽게 접히도록 조정했다.

## 2026-07-07
- `ads.txt`: AdSense 게시자 ID `pub-5536720144858364`를 Google 공식 ads.txt 형식으로 루트에 추가했다.
- `index.html`, `style.css`, `i18n.js`, `about.html`, `privacy.html`, `terms.html`, `contact.html`, `affiliate-disclosure.html`, `guides/*.html`, `robots.txt`, `sitemap.xml`: AdSense의 `가치가 별로 없는 콘텐츠` 거절 사유를 기준으로 루트 페이지에 사이트 설명/가이드 허브 링크/canonical/구조화 데이터를 추가하고, 크롤링 가능한 독립 가이드 5개와 소개/문의/개인정보처리방침/이용약관/제휴고지 페이지를 추가했다. 표준이 아닌 복사성 `adsense.txt`는 삭제하고 `robots.txt`, `sitemap.xml`을 배포 대상에 추가했다.
- `index.html`, `style.css`, `i18n.js`: 모바일 App Guide의 더 읽기 본문 표가 카드 폭 밖으로 밀리는 문제를 수정했다. 번역 적용 후 표 헤더를 각 셀의 모바일 라벨로 주입하고, 작은 화면에서는 표를 행 단위 카드 레이아웃으로 전환해 Abrahang 가이드 표가 우측으로 넘치지 않도록 했다.
- `index.html`, `abrahang-timer.js`: Abrahang 훈련 히스토리 목록의 하중 칸을 기존 목표하중 단독 표시에서 `목표하중 / 수행평균부하` 형식으로 바꿨다. 수행평균부하는 새 기록의 세트별 샘플 합계/샘플 수를 우선 사용하고, 샘플 합계가 없는 기존 기록은 그립/손별 샘플 또는 세트별 최대하중 평균으로 폴백한다.
- `index.html`, `main.js`: 탭 공유 버튼이 `?tab=abrahang#abrahang`처럼 hash를 포함한 주소를 만들지 않고 `?tab=abrahang` 형태의 깨끗한 공유 URL만 복사하도록 수정했다. Instagram/Facebook이 기존 링크의 `#abrahang`를 `%23abrahang`로 query 안에 붙여도 `tab` 값에서 `#` 뒤를 잘라 정상 Abrahang 탭으로 들어가도록 URL 파서를 보강했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: Abrahang의 별도 `오늘의 순서` 영역을 제거하고, 타이머 하단 현재 동작 설명 박스 안에 세트 묶음만 짧은 칩 형태로 표시하도록 바꿨다. 기존 상세 설명 리스트는 없애고, 현재/대기/휴식/완료 상태에 맞춰 해당 묶음만 강조한다.
- `index.html`, `style.css`: 모바일 Abrahang 화면에서 강도 설정 영역이 훈련 히스토리보다 먼저 나오도록 히스토리를 타이머 패널 내부에서 독립 그리드 영역으로 옮겼다. 데스크톱에서는 히스토리가 계속 왼쪽 하단에 남고, 모바일에서는 `타이머 -> 강도 설정 -> 훈련 히스토리` 순서로 표시된다.
- `index.html`, `style.css`: 모바일 Abrahang 화면에서 `로딩/휴식/반복/다음 세션` 요약 카드가 1열로 쌓여 첫 화면을 과도하게 차지하던 문제를 수정했다. 모바일에서도 4개 요약을 가로 4열로 유지하고 패딩/글자 크기를 줄여 세로 높이를 낮췄다. 안전 기준은 데스크톱에서는 기존 설정 패널에 유지하고, 모바일에서는 오늘의 순서 아래 App Guide 바로 위에 별도 표시되도록 배치했다.
- `index.html`, `style.css`, `abrahang-timer.js`: 모바일 화면에서 타이머를 보며 실시간 하중도 확인할 수 있도록 링 타이머 상단 좌우에 현재 하중/피크 하중 배지를 추가했다. 배지는 모바일 폭에서만 표시되며, 기존 하단 좌우 그립 이미지와 상하 대칭이 되도록 같은 다이얼 스테이지 안에 배치했다. 현재 하중은 크레인 저울에서는 저울값 자체, 발판 저울에서는 체중-저울값 기준 실제 하중을 표시한다.
- `abrahang-timer.js`, `style.css`, `i18n.js`, `index.html`: 훈련 히스토리 상세 드롭다운에 `그립별 평균 하중` 영역을 추가했다. 각 그립 카드에는 좌/우 손 아이콘을 배치하고, 새로 기록되는 세션은 수신 샘플 합계/샘플 수 기준 평균 하중을 표시한다. 기존 기록처럼 샘플 합계가 없는 경우에는 저장된 세트별 최대하중의 평균으로 폴백해 현재 기록된 데이터에서 가능한 평균을 보여준다.
- `abrahang-timer.js`, `i18n.js`: 아브라행 훈련 히스토리의 측정 결과를 손별로 분리해 저장하도록 확장했다. 한손 모드에서는 현재 수행 손의 그립별 최대하중/달성률만 해당 손에 누적하고, 양손 모드에서는 같은 측정치를 왼손/오른손 양쪽에 동일하게 기록한다. 결과분석 그래프는 기존 그립별 4개 라인에서 좌/우 손을 포함한 8개 라인으로 확장했으며, 히스토리 상세에는 왼손/오른손 최대 하중을 함께 표시한다. WH-C06 페이지 새로고침 복구 payload에도 손별 측정 누적값을 포함한다.
- `index.html`, `style.css`, `abrahang-timer.js`, `assets/images/abrahang-grip-candidates/20260706/*-display.webp`: 기존 그립 PNG 원본은 백업용으로 유지하고, 실제 앱 표시에는 320px 정사각형 WebP 표시본을 사용하도록 바꿨다. 표시본은 현재 UI에서 잘려 보이는 손+행보드 중심 영역만 크롭해 생성했으며, 각 파일은 약 6.8~7.8KB 수준이다. CSS의 기존 132% 확대/이동 크롭은 제거하고 WebP 표시본을 그대로 채우게 했다.

## 2026-07-06
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06의 같은 페이지 내 `다시 연결`/`재시작` 동선이 실제 Chrome Web Bluetooth 광고 감시 복구에 실패한다는 관측을 기준으로, WH-C06 세션이 이미 있거나 수신이 멈춘 상태에서는 버튼을 `페이지 새로고침`으로 표시하도록 단순화했다. 최초 연결 전에는 기존처럼 `WH-C06 / IF_B7 연결(beta)`로 장치 목록을 열고, 이후 끊김/첫 패킷 없음/복귀 후 수신 없음 상태에서는 타이머 상태를 `sessionStorage`에 저장한 뒤 페이지 새로고침으로 Web Bluetooth 실행 컨텍스트를 새로 만드는 안내와 동작을 사용한다. 별도 `WH-C06 완전 재접속` 버튼은 제거했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 블루투스 저울 패널 안의 크레인 저울 구매 링크/Chrome flags 안내와 오른쪽 패널의 `다음 권장 시작` 카드 위치를 서로 교환했다. WH-C06의 같은 페이지 내 `다시 연결`/기존 감시 재시작이 실제 기기에서 계속 실패한다는 관측에 맞춰, 끊김 상태에서는 `WH-C06 완전 재접속` 버튼을 추가로 표시한다. 이 버튼은 현재 타이머 세션 상태를 `sessionStorage`에 임시 저장한 뒤 Abrahang 탭을 새로 열어 Web Bluetooth 실행 컨텍스트 자체를 초기화하고, 새로고침 후 타이머 상태를 복원한 다음 WH-C06 연결을 다시 누르게 한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 일반 방문자가 WH-C06 수신 기능을 준비할 수 있도록 Chrome flags 안내 블록을 블루투스 저울 영역에 추가했다. `chrome://flags/#enable-experimental-web-platform-features` 열기 링크와 주소 복사 버튼을 함께 제공하며, Chrome이 내부 설정 주소 이동을 막는 경우 새 탭 주소창에 붙여넣도록 설명한다. Web Bluetooth 부분 reload 우회는 단순 DOM 교체로는 불가능하고, 저울 연결부를 같은 origin iframe으로 분리한 뒤 iframe 자체를 갈아끼우는 방식만 의미가 있다는 판단을 남겼다. 이 방식은 iframe 안 버튼 클릭으로 `requestDevice()` 사용자 활성화를 만족시키고 parent와 `postMessage`로 하중값을 주고받는 별도 구조 변경이 필요하다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 창 복귀 후 `WH-C06 다시 연결`에서 장치 목록은 열리지만 선택 후 광고 패킷이 들어오지 않는 문제를 다시 좁혔다. 기존 구현은 장치 선택창을 열기 전 이전 `watchAdvertisements()`를 완전히 정리하지 않았고, 첫 패킷 타임아웃 후 다시 `재시작` 루프로 돌아갔다. 재연결 클릭 시 이전 WH-C06 감시 해제를 먼저 시작하고 장치 선택 후에도 한 번 더 `unwatchAdvertisements()`를 강제한 뒤 새 감시를 붙이도록 바꿨으며, 첫 패킷 타임아웃 후에도 `WH-C06 다시 연결`로 장치 목록을 다시 여는 상태를 유지하게 했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 기존 장치 감시 재시작이 실제 기기에서 복구되지 않는 상황에 맞춰, 창 복귀/수신 끊김 감지 시 자동 재시작 대신 `다시 연결` 상태로 전환하도록 바꿨다. 이 상태에서 버튼을 누르면 `requestLEScan()`이나 기존 장치 재시작이 아니라 최초 연결과 같은 `requestDevice()` 장치 목록을 다시 열어 IF_B7을 재선택하게 한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 창 전환 직후 WH-C06 수신 끊김을 앱이 감지하는 점을 복구 트리거로 사용하도록 바꿨다. 기존 WH-C06 장치 객체가 있으면 버튼 클릭이나 Chrome 복귀/수신 끊김 감지 시 새 권한창이나 장치 목록을 열지 않고 기존 장치의 광고 감시를 `unwatchAdvertisements()` 후 `watchAdvertisements()`로 재시작한다. 새 BLE 광고 스캔은 기존 장치 객체가 없는 leScan 세션에만 남겨둔다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 최초 연결에서 `requestLEScan()` 권한창이 떠 장치 목록이 보이지 않는 문제를 수정했다. WH-C06 세션이 아직 없을 때는 기존처럼 Chrome 장치 목록(`requestDevice`)에서 IF_B7을 선택하게 하고, 이미 WH-C06 세션이 있는 상태에서 끊긴 뒤 누르는 새로 스캔에만 BLE 광고 스캔 복구 경로를 사용한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 새로 스캔이 여전히 페어링된 장치 캐시/기존 장치 감시 상태에 묶일 수 있어, 지원 브라우저에서는 `requestLEScan({ keepRepeatedDevices: true })` 기반의 새 BLE 광고 스캔을 우선 사용하도록 바꿨다. 새 BLE 스캔은 모든 광고를 받아 앱 내부에서 WH-C06 manufacturer data만 필터링하며, 해당 API를 사용할 수 없는 경우에만 기존 `requestDevice()`/`watchAdvertisements()` 경로로 폴백한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 새로 스캔을 페이지 새로고침 직후 첫 연결과 같은 흐름에 가깝게 바꿨다. 버튼 클릭 시 먼저 Chrome 장치 선택창을 열어 사용자 제스처를 보존하고, 선택 후 기존 광고 감시를 충분히 정리한 뒤 새 `watchAdvertisements()` 세션을 시작한다. Chrome 복귀/페이지 복원 시에는 기존 감시를 끊지 않고 수신 재확인만 수행하며, 관련 안내 문구도 실제 동작 기준으로 수정했다.
- `index.html`, `style.css`, `i18n.js`: 아브라행 블루투스 저울 패널에 크레인 저울 제휴 링크를 작은 장비 링크 형태로 추가했다. 트레이닝 조작을 방해하지 않도록 측정값 아래 설명문 위에 배치하고, `제휴 링크` 표기를 함께 표시한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 훈련 히스토리 상세 드롭다운에 `결과분석` 체크박스를 추가하고, 체크한 세션만 시간순으로 모아 4손가락/앞 3손가락/앞 2손가락/중간 2손가락의 최대하중과 하중달성률을 캔버스 그래프로 표시하도록 했다.
- `abrahang-timer.js`: 세션 완료 기록에 전체 최대하중뿐 아니라 1~20세트별 측정 최대하중과 그립별 요약 측정치를 함께 저장하도록 확장했다. 기존 히스토리에 그립별 데이터가 없으면 분석 그래프에서는 측정 데이터 없음 상태로 처리한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 링 타이머 좌우 바깥쪽 하단에 손가락 그립 개별 PNG를 배치했다. 오른쪽은 원본 오른손 이미지를 사용하고, 왼쪽은 CSS로 반전해 표시한다.
- `abrahang-timer.js`: 현재/다음 세트의 그립 종류에 맞춰 4손가락, 앞 3손가락, 앞 2손가락, 중간 2손가락 이미지를 전환한다. 한손 모드에서는 선택한 손을 기준으로 라운드마다 좌우를 교대하며, 프리카운트/휴식 중에는 다음 수행 손을 미리보기 점멸로 표시하고 실제 로딩 중에는 수행 손을 활성 점멸로 표시한다.
- `assets/images/abrahang-grip-candidates/20260706/*.png`: 앱에서 사용할 개별 손가락 그립 PNG 편집본을 반영했다. `Hangboard_fingers.psd` 작업 원본은 커밋 대상에서 제외한다.

## 2026-07-05
- `index.html`, `style.css`, `abrahang-timer.js`: 아브라행 `오늘의 순서`에서 인접한 동일 세트를 `1~6` 같은 범위 묶음으로 렌더링하도록 바꿨다. 현재 진행/휴식 미리보기/완료 상태는 해당 세트 범위가 포함된 묶음 카드에 적용되며, CSS/스크립트 캐시 버전을 갱신했다.

## 2026-04-02
- `index.html`: 브라우저 탭 아이콘이 표시되도록 favicon 및 apple touch icon 링크를 추가했다.
- `assets/icons/favicon-16x16.png`: 원본 보석 이미지를 16x16 파비콘으로 리사이즈해 추가했다.
- `assets/icons/favicon-32x32.png`: 원본 보석 이미지를 32x32 파비콘으로 리사이즈해 추가했다.
- `assets/icons/favicon-48x48.png`: 원본 보석 이미지를 48x48 파비콘으로 리사이즈해 추가했다.
- `assets/icons/favicon.ico`: 브라우저 호환용 멀티사이즈 ico 파일을 추가했다.
- `assets/icons/apple-touch-icon.png`: 모바일 홈 화면용 180x180 아이콘을 추가했다.

## 2026-04-03
- `index.html`: 헤더 왼쪽에 파비콘 이미지를 홈 버튼처럼 배치하고, 스타일/스크립트 캐시 버전을 갱신했다.
- `style.css`: 헤더 홈 아이콘 버튼 스타일을 추가해 기존 중앙 타이틀 레이아웃을 유지하면서 홈 이동 동선을 만들었다.
- `main.js`: 호흡트레이너 사운드 재생이 크롬에서 실패해도 Web Audio 톤으로 폴백되도록 오디오 초기화와 재생 로직을 보강했다.

## 2026-06-19
- `index.html`: CO2 테이블 스톱워치의 `Auto`/`Lap` 버튼을 제거하고 숨참기 홀드시간, 휴식시간 설정 UI를 추가했다.
- `stopwatch.js`: `Start` 버튼이 설정값 기반 자동 CO2 루틴을 시작하도록 바꾸고, 휴식 시간이 라운드마다 15초씩 감소하되 15초 아래로 내려가지 않도록 했다.
- `style.css`: CO2 테이블 설정 입력과 5초 증감 버튼 레이아웃을 추가했다.
- `i18n.js`: CO2 테이블 자동 진행, 숨참기/휴식 설정, 차트/기록 문구를 한글/영문으로 갱신했다.
- `stopwatch.js`, `style.css`: CO2 차트 링 확장 효과가 찌그러지거나 캔버스 밖에서 잘리지 않도록 차트 내부 패딩과 캔버스 표시 비율을 조정했다.
- `main.js`, `stopwatch.js`: Android Chrome 등 Screen Wake Lock 지원 브라우저에서 호흡/CO2 타이머 작동 중 화면이 꺼지지 않도록 wake lock 요청/해제 흐름을 추가했다.
- `stopwatch.js`, `style.css`: CO2 기록 영역의 활성 칸 점멸 효과를 남은 시간에 따라 오른쪽부터 줄어드는 게이지 배경으로 변경했다.
- `style.css`: CO2 기록 영역의 게이지 배경을 유지하면서 활성 칸 점멸 효과가 함께 보이도록 펄스 애니메이션을 복원했다.
- `index.html`, `style.css`: CO2 테이블 시간 설정의 상단 라벨 줄을 숨기고 중앙 입력칸 배경에 `hold`/`rest` 표기를 넣어 설정 영역 높이를 줄였다.
- `main.js`, `stopwatch.js`, `index.html`: CO2 테이블 자동 루틴이 끝나거나 정지/리셋될 때 `running` 상태 전환과 함께 Screen Wake Lock이 확실히 해제되도록 정리하고 스크립트 캐시 버전을 갱신했다.
- `index.html`, `style.css`: CO2 시간 설정 중앙 입력 영역에서 `hold`/`rest`와 시간값을 좌우 배치로 바꾸고, 차트/게이지 색상과 맞춰 hold는 붉은 계열, rest는 푸른 계열 배경으로 조정했다.
- `index.html`, `style.css`: CO2 시간 설정 중앙 입력 영역에서 `hold`/`rest` 옆 시간값을 각 값 칸 안에서 가운데 정렬되도록 조정하고 스타일 캐시 버전을 갱신했다.
- `stopwatch.js`, `i18n.js`, `index.html`: CO2 자동 루틴이 휴식 15초에 도달해도 자동 종료되지 않고, 이후 라운드는 15초 휴식으로 계속 진행되도록 수정하고 안내 문구와 스크립트 캐시 버전을 갱신했다.
- `main.js`, `index.html`: 호흡타이머 사운드를 파일 재생 대신 Web Audio 합성 톤 기본값으로 바꾸고, 소리 크기 슬라이더가 합성 톤의 master gain에 직접 적용되도록 정리했다.
- `index.html`: URL에 유효한 탭 값이 없을 때 빈 selector로 탭 검증을 시도하지 않도록 방어 로직을 보강했다.

## 2026-06-24
- `index.html`, `style.css`, `i18n.js`: 호흡 타이머 컨트롤에 `자동 +0.1초` 토글 버튼을 추가하고 한국어/영어 문구와 활성 상태 스타일을 연결했다.
- `main.js`: 날숨이 끝나고 다음 들숨 라운드로 넘어갈 때 라운드 번호를 증가시키고, 자동 토글이 켜져 있으면 들숨/날숨 시간을 라운드마다 0.1초씩 증가하도록 했다.
- `index.html`: 자동 0.1초 증가값이 유효한 숫자 입력으로 유지되도록 들숨/날숨 입력 step을 0.1초로 조정하고 스크립트 캐시 버전을 갱신했다.
- `index.html`, `style.css`, `main.js`, `i18n.js`: 자동 버튼 좌우에 증가량 조절용 꺾쇠 버튼을 추가하고, 자동 증가량을 0.1초 단위로 조절하되 0.1초 이하로 내려가지 않도록 왼쪽 버튼 비활성 상태를 적용했다.

## 2026-06-25
- `index.html`, `style.css`, `main.js`, `i18n.js`: 호흡 트레이너 원형 게이지 중앙에 현재 단계의 목표 설정시간을 진행시간보다 작은 글씨로 표시하고, 입력값/단계/언어 변경 시 즉시 갱신되도록 했다.
- `main.js`, `index.html`: 호흡 사운드 전환/정지 시 기존 oscillator를 즉시 끊지 않고 짧게 릴리즈 페이드아웃하도록 바꾸고, 새 톤도 부드러운 어택으로 시작하게 조정해 팝 노이즈 원인을 제거했다. 날숨 톤은 거친 triangle 파형 대신 sine 파형의 하강 톤으로 바꾸고, 볼륨 슬라이더 변경도 짧게 스무딩하도록 했다.

## 2026-06-26
- `index.html`, `style.css`: 호흡 트레이너 설정 영역을 들숨/날숨 각각 한 줄 구성으로 압축하고, 원형 게이지 안 목표시간 글자 크기를 키웠다.
- `index.html`, `style.css`, `main.js`, `i18n.js`: 호흡 트레이너의 `소리 크기` 라벨을 `주파수`로 바꾸되 볼륨 슬라이더는 유지하고, 차크라 선택 이미지를 추가해 선택한 차크라의 솔페지오 Hz를 날숨 톤으로, 2배 배음을 들숨 톤으로 적용하도록 했다.
- `main.js`, `index.html`: 차크라 주파수 적용 후 사라진 호흡 사운드 시작부 피치 업/다운 효과를 복원하고, 캐시 버전을 갱신했다.
- `main.js`, `index.html`, `i18n.js`: 들숨 사운드의 2배 배음 적용을 제거하고, 날숨과 동일하게 선택한 차크라의 원래 Hz로 재생/표시되도록 수정했다.
- `index.html`, `i18n.js`: 차크라 선택 영역에서 들숨/날숨 주파수를 따로 표기하지 않고 `차크라명 · Hz` 형식으로만 표시되도록 정리했다.
- `index.html`, `i18n.js`: 호흡 타이머 앱 가이드에 차크라 주파수 포스트를 추가하고, 더보기 본문에 차크라별 위치/의미/색/만트라/솔페지오 Hz와 사용 주의점을 정리했다.

## 2026-07-02
- `index.html`, `style.css`, `i18n.js`: 호흡 트레이너의 차크라 주파수 선택기를 세로형으로 바꾸고 원형 게이지 왼쪽에 배치했다. 볼륨 슬라이더는 설정 패널 가운데 정렬하고 레이블을 `볼륨`으로 복구했다.
- `index.html`: 세로형 차크라 선택기의 시각 순서를 인체 위치 기준에 맞춰 사하스라라/Crown이 위, 물라다라/Root가 아래로 보이도록 정렬했다.
- `index.html`, `style.css`: 호흡 트레이너 상단을 `주파수 선택기 / 원형 게이지 / 세로 볼륨 슬라이더` 3열 구조로 정리해 원형 게이지가 가운데 축에 오도록 조정했다.
- `index.html`, `style.css`: 세로 볼륨 슬라이더가 사이트 공통 초록 accent를 상속받지 않도록 볼륨 슬라이더 색상을 기존 파란색 계열로 복구하고 스타일 캐시 버전을 갱신했다.
- `index.html`: 새 `abrahang` 탭과 앱 가이드 글을 추가하고, 아브라행 행보드 타이머 스크립트와 캐시 버전을 연결했다.
- `abrahang-timer.js`: 논문 기반 10초 로딩/20초 휴식 20회 프로토콜과 영상 기반 10초 로딩/50초 휴식 10회 프로토콜을 전환할 수 있는 아브라행 타이머를 추가했다. 전환음, 루틴 복사, 강도 목표, 완료 후 6시간 회복 기록을 포함했다.
- `style.css`: 아브라행 타이머 전용 원형 진행 게이지, 프로토콜 전환 버튼, 안전 기준, 순서표, 모바일 레이아웃 스타일을 추가했다.
- `i18n.js`: 아브라행 타이머 탭/버튼/상태/가이드 문구를 한국어와 영어 번역 키로 추가하고, 관련 논문과 요청 영상을 가이드 출처로 연결했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 타이머 설정에 체중 입력을 추가하고, 강도 목표 퍼센트를 체중 기준 kg 목표 하중으로 즉시 환산해 표시하도록 했다. 입력한 체중은 브라우저 로컬 저장소에 보존된다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 강도 목표 범위를 체중 30~80%로 확장하고, 논문 모드는 50%, Emil 모드는 70%를 기본값으로 자동 적용하도록 했다.
- `abrahang-timer.js`, `style.css`, `index.html`: 아브라행 휴식 중 상태 카드가 다음 세트 미리보기를 표시하도록 바꾸고, 오늘의 순서 하이라이트가 진행 단계에 맞춰 이동하도록 수정했다. 실제 로딩 중인 세트에만 점멸 효과를 적용하고 휴식 중 프리뷰는 비점멸 하이라이트로 구분했다.
- `index.html`, `i18n.js`: 아브라행 논문 모드 표시명을 `논문 10분`/`Paper 10 min`에서 `Abrahangs`로 통일하고 i18n 캐시 버전을 갱신했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 `루틴 복사` 버튼과 클립보드 복사 로직을 제거하고, 시작/안전 기준 문구를 하중 통제와 가벼운 워밍업 기준에 맞춰 수정했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 설정에 `저울 연결(beta)` 패널을 추가하고, 표준 BLE Weight Scale Service 연결/알림 수신/실시간 저울값 표시와 체중 대비 실제 손가락 하중 계산을 연결했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 사용 중인 Weiheng WH-C06/IF_B7 행잉 스케일이 표준 Weight Scale Service가 아니라 광고 패킷으로 무게를 보내는 구조라서, WH-C06 전용 연결 버튼과 `watchAdvertisements()` 기반 manufacturer data 파싱 경로를 추가했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: WH-C06 연결 후 긴 오류 안내가 버튼 영역에 눌려 한 글자씩 줄바꿈되는 문제를 막기 위해 저울 패널 헤더를 한 열 구조로 고정하고, 상태 문구는 짧게, Chrome flag 안내는 설명 문구로 분리했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: WH-C06가 목록에는 뜨지만 실시간 저울값이 나오지 않는 경우를 구분하기 위해 브라우저의 `watchAdvertisements()` 지원 여부를 기기 선택 전에 진단하고, 미지원 브라우저에서는 Chrome 실험 기능 활성화/완전 재시작 안내를 표시하도록 했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 강도 설정에 한손 모드를 추가해 목표 하중을 절반으로 계산하도록 하고, 저울 방식을 발판 저울/크레인 저울로 분리했다. 크레인 저울 모드에서는 저울값을 현재 하중으로 표시하고 목표 저울값 대신 목표 하중을 표시하며, WH-C06 광고 패킷 대기 시간은 10초에서 1분으로 늘렸다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 광고 이벤트를 받은 즉시 수신 타이머를 갱신하도록 바꾸고, 무게값이 없는 광고 패킷/수신 공백/1분 이상 미수신 상태를 분리해 표시했다. 수신 공백이 길어지거나 탭이 다시 표시되면 연결을 끊지 않고 `watchAdvertisements()` 감시를 재시작하도록 보강했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 타이머 최초 시작 시 3초 프리카운트와 카운트다운/시작 신호음을 거친 뒤 첫 로딩 세트가 시작되도록 했다. WH-C06는 수신 공백이 생겨도 자동 `unwatchAdvertisements()`를 호출하지 않고 감시를 유지하며, 이미 선택된 WH-C06에서 버튼을 누르면 장치 선택창 대신 현재 장치의 광고 감시만 다시 확인하도록 바꿨다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 강도 안내 문구의 `논문 모드` 표기를 `Abrahangs 모드`로 수정했다. WH-C06 광고 수신이 길게 멈추면 더 이상 연결 중으로 표시하지 않고 수신 중단/새로 스캔 필요 상태로 전환하며, WH-C06 버튼은 기존 장치 객체 재사용 대신 새 장치 선택 스캔을 열도록 변경했다.
- `index.html`, `abrahang-timer.js`: 실제 아브라행 수행 중 20초 휴식 구간에서 WH-C06가 광고 패킷을 보내지 않을 수 있어 수신 중단 판정 시간을 90초로 늘렸다. 프리카운트 종료 및 각 휴식 종료 후 로딩에 들어갈 때 `watchAdvertisements()`를 다시 확인해 브라우저 광고 감시가 조용히 멈춘 경우를 복구하도록 했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 실제 수행 중 행 로딩 타이밍에 WH-C06 값 수신이 멈추는 경우를 보강했다. 로딩 중 4초 이상 광고 패킷이 없으면 `navigator.bluetooth.getDevices()`로 Chrome에 이미 허가/페어링된 IF_B7 장치를 찾아 새 `BluetoothDevice` 객체로 `watchAdvertisements()`를 다시 시작하고, 해당 상태를 저울 패널에 표시하도록 했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 새로 스캔 버튼이 페어링된 장치 캐시를 먼저 재사용해 실제 스캔이 열리지 않는 문제를 수정했다. 수동 WH-C06 버튼은 항상 Chrome 장치 선택창을 열고, 자동 복구에서만 페어링된 IF_B7 재감시를 시도한다. 광고 감시 재시작 시 `unwatchAdvertisements()` 완료를 기다린 뒤 `watchAdvertisements()`를 호출해 늦은 unwatch가 새 watch를 끊는 상황을 줄였고, 자동 복구 후 12초 동안 첫 패킷이 없으면 새 스캔 필요 상태로 전환하도록 했다.
- `index.html`, `abrahang-timer.js`: 아브라행 카운트다운/전환 신호음의 기존 Web Audio 게인이 카운트다운 `0.12`, 전환/완료 `0.18`로 최대값이 아니어서, 신호음 게인을 `1.0`으로 상수화해 최대 볼륨으로 재생되도록 조정하고 스크립트 캐시 버전을 갱신했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: Chrome에서 다른 프로그램으로 전환했다가 돌아온 뒤 WH-C06 광고 수신이 멈추고 새 스캔으로도 값이 복구되지 않는 상황을 보강했다. `visibilitychange`와 `window blur/focus`를 함께 감지해 Chrome 복귀 후에는 기존 광고 감시를 `unwatch`로 끊지 않고 감시만 확인하며, 쉬는 구간에서는 실패 판정을 미루고 다음 로딩 단계에서 새 패킷 도착을 15초 동안 확인한다. 수동 WH-C06 새 스캔은 기존 WH-C06 객체의 `unwatchAdvertisements()`를 기다리지 않고 로컬 상태를 분리한 뒤 Chrome 장치 선택창을 열도록 바꿨다.

## 2026-07-03
- `index.html`, `style.css`: 로또번호 생성기 액션 영역에 눈에 덜 띄는 `수동입력` 버튼과 45개 번호 선택 패널을 추가하고, 6개 선택 완료 시에만 `심상화` 비밀 버튼이 나타나도록 구성했다. 호흡 타이머 원형 게이지 아래에는 선택한 번호 6개를 표시하는 숨김 영역을 추가했다.
- `lottery-generator.js`: 수동 선택 상태, 6개 번호 검증, 브라우저 로컬 저장소 저장, `lottery:vision` 이벤트 발행, 호흡 타이머 탭 전환 흐름을 추가했다.
- `main.js`: 로또 수동 번호 저장값과 `lottery:vision` 이벤트를 받아 호흡 타이머 원형 게이지 아래에 번호볼을 렌더링하도록 연결했다.
- `i18n.js`: 로또 수동입력/심상화 버튼과 호흡 타이머 심상화 번호 라벨 번역 키를 추가했다.
- `docs/현재작업중.md`: 이번 코드 변경 파일과 작업 내용을 기록했다.
- `index.html`, `style.css`, `i18n.js`, `lottery-generator.js`: 로또 수동입력의 `지우기` 버튼을 `저장하기`로 바꾸고, 저장된 6개 번호 묶음을 리스트로 관리하도록 수정했다. 저장 리스트 행을 선택하면 희미한 `심상화` 버튼이 나타나며, 행별 삭제 버튼으로 리스트 항목만 삭제할 수 있다.
- `main.js`, `lottery-generator.js`: 심상화 번호는 더 이상 브라우저 저장소에서 호흡 타이머로 복원하지 않고, 저장 리스트의 `심상화` 버튼을 누른 순간에만 `lottery:vision` 이벤트로 전달하도록 변경했다. 새로고침 또는 다른 앱 탭 이동 시 호흡 타이머의 심상화 표시는 사라진다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: WH-C06 연결 중 Chrome 포커스가 다른 프로그램으로 넘어가면 타이머를 자동 일시정지하고, 복귀 후 새 WH-C06 패킷을 다시 확인하도록 했다. 아브라행 완료 시 프로토콜, 목표 강도, 목표 하중, 최대 측정 하중, 하중달성률, 날짜/시간을 훈련 히스토리에 저장하고, 기존 완료 시각 로그는 측정누락 히스토리로 1회 마이그레이션한다. 히스토리 항목은 리스트에서 편집/삭제/전체 삭제할 수 있다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 포커스 이탈 대응에서 타이머 자동 일시정지를 제거했다. Chrome/Web Bluetooth 광고 스캔은 포커스 이탈 시 중단될 수 있으므로 타이머는 계속 진행하고, Chrome 복귀/페이지 표시/포커스 이벤트에서 WH-C06 광고 감시를 즉시 재확인하도록 변경했다. 안내 문구도 타이머 지속과 복귀 후 재감시 기준으로 수정했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 수신이 멈춘 뒤 새로 스캔해도 페이지 새로고침 전까지 값이 돌아오지 않는 문제를 줄이기 위해 수동 WH-C06 새 스캔 경로를 강제 재시작 방식으로 바꿨다. 장치 선택 후 기존 광고 감시를 `unwatchAdvertisements()`로 끊고 짧은 간격 뒤 새 `watchAdvertisements()`를 시작하며, 수동 재스캔 후 20초 동안 새 패킷이 없으면 저울 전원 재시작 안내를 표시한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 훈련 히스토리를 훈련명과 날짜/시간 중심의 접이식 리스트로 바꿨다. 목표 강도와 목표 하중은 `목표 설정` 한 항목으로 묶어 중복 노출을 줄이고, 설정값 누락은 `설정누락`, 실제 측정값 누락은 `측정누락`으로 구분한다. 최대 측정 하중과 하중달성률은 항목을 클릭/터치했을 때만 상세로 표시된다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 저울 패널의 중복 `목표 하중` 칸을 `피크 하중`으로 바꾸고, 표준 저울/WH-C06 수신값에서 계산한 실제 하중의 최대값을 유지 표시하도록 했다. 피크 하중은 새 타이머 시작, 초기화, 새 저울 연결, 저울 방식/체중 변경 시 초기화된다.
- `index.html`, `abrahang-timer.js`: 아브라행 강도 목표 슬라이더 값을 브라우저 로컬 저장소에 보존해 새로고침 후에도 마지막 설정값이 유지되도록 했다. 저장값이 없을 때만 기존 Abrahangs 기본 50%를 적용하고, 모드 버튼을 새로 누르면 해당 모드 기본값을 다시 저장한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 수신이 작업 전환/스크린샷 직후 먹통이 되는 문제를 줄이기 위해 `blur`/`focus`/`visibilitychange`/`pagehide`를 WH-C06 복구 트리거로 쓰지 않도록 변경했다. 프리카운트 종료와 세트 전환 때 자동으로 `watchAdvertisements()`를 다시 호출하던 경로도 제거해, 작업 전환은 앱 내부에서 수신 오류나 재감시 상태를 만들지 않고 기존 광고 감시를 유지한다.
- `index.html`, `style.css`, `abrahang-timer.js`: 아브라행 훈련 히스토리를 오른쪽 설정 패널에서 왼쪽 타이머 패널 하단으로 옮기고, 리스트 요약을 `훈련명 / 하중(kg) / 날짜` 순서로 단순화했다. 레거시 기록도 `기존 로그` 접미사를 표시하지 않고, 목표 하중이 없으면 `설정누락`으로 표시한다.
- `index.html`, `abrahang-timer.js`: 아브라행/Emil 타이머에서 마지막 로딩 세트 뒤에는 휴식 타이머로 넘어가지 않고 즉시 완료되도록 총 시간 계산과 단계 전환을 수정했다. 완료 알람은 긴 비프 3회 패턴으로 바꿨다.
- `lottery-generator.js`: 로또번호 생성기를 1~10, 11~20, 21~30, 31~40, 41~45 구간 프로파일 기반의 디퓨전식 생성 흐름으로 확장했다. 과거 회차의 구간 패턴/전이/최근 흐름/쌍 조합 점수를 바탕으로 노이즈 번호를 12단계 복원하고, 최종 5세트를 선별한다.
- `index.html`, `style.css`, `i18n.js`: 로또 알고리즘 패널에 `구간 디퓨전` 프로파일 표시와 복원 단계 표시를 추가하고, 생성하기 연출이 노이즈 복원 진행률을 보여주도록 문구와 스타일을 갱신했다.
- `lottery-generator.js`, `style.css`, `i18n.js`, `index.html`: 디퓨전 연출이 6개 번호만 빠르게 깜빡이지 않도록 바꾸고, 45개 전체 후보 풀이 천천히 줄어든 뒤 살아남은 번호들이 A~E 5개 세트 슬롯으로 배분되는 흐름으로 개선했다.
- `assets/images/chakra-body-outline.png`, `index.html`, `style.css`: 이미지 생성으로 만든 명상 인체 외곽선 자산을 차크라 선택기 배경에 배치하고, 1~7번 차크라 버튼을 인체 중심선의 위치에 맞춰 표시하도록 조정했다.
- `assets/images/chakra-body-outline-right.png`, `index.html`, `style.css`: 차크라 선택기에서 버튼 축과 인체 외곽선 이미지 영역을 분리하고, 오른쪽 반신 전용 보정 이미지를 사용해 실루엣이 보이도록 했다. 1~7번 버튼 간격도 넓혀 겹침을 해소했다.
- `index.html`, `style.css`: 호흡타이머 차크라 선택기 실루엣 배경을 이전보다 더 크게 아래로 내리고 세로 크기를 유지해 7번 버튼이 머리 위, 1번 버튼이 하단/발 위치에 더 가깝게 오도록 조정했다.

## 2026-06-29
- `index.html`: 새 `lottery` 탭과 앱 가이드 글을 추가하고, 로또번호 생성기 스크립트/캐시 버전을 연결했다.
- `style.css`: 로또번호 생성기 결과 세트, 번호볼, 데이터 집계, 알고리즘 설명 패널의 반응형 스타일을 추가했다.
- `i18n.js`: 로또번호 생성기 탭/버튼/상태/가이드 문구를 한국어와 영어 번역 키로 추가했다.
- `lottery-generator.js`: 번들 이력, 브라우저 캐시, 공식 API, 공개 JSON 보조 소스를 조합해 로또 6/45 당첨번호 데이터를 갱신/저장하고 5개 추천 세트를 생성하도록 추가했다.
- `assets/data/lotto-645-history.json`: 앱 초기 로딩용 로또 6/45 역대 당첨번호 스냅샷을 추가했다.
- `index.html`, `style.css`, `i18n.js`, `lottery-generator.js`: 로또 탭 첫 진입 시 추천번호가 자동 노출되지 않게 바꾸고, `생성하기` 클릭 후 번호볼 뽑기 연출을 거쳐 결과가 표시되도록 수정했다. 타이틀 아래 중복 설명과 더보기의 데이터 로딩 설명을 줄이고 알고리즘 요약만 남겼다.
