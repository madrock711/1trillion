# 현재 작업중

## 2026-08-06
- `articles/market-2026-08-06.html`, `assets/data/market-dashboard-20260806-1005.json`: 공개 화면 검수에서 최종 시장 폭의 상승 416개·하락 455개 방향을 바로잡았다. 지수 -4.35%에 비해 종목 수 격차가 39개로 제한된 대형 반도체 집중 하락으로 해석을 교정하고 대응 점수를 공격 20·관망 50·방어 30으로 통일했다.
- `reports/2026-08-06-1005.md`, `STATE.md`, `charts/us_yield_spreads_long_term_2026-08-06.png`, `charts/us_yield_spreads_90d_2026-08-06.png`: 미국 AI·반도체의 Nvidia 독주와 AMD·SOXX 하락, ADP·ISM 서비스업, 최신 DRAM 현물가와 미국 국채 확정치를 조사한 뒤 발행 직전 10:05 KST KST의 한국 지수·수급·프로그램·환율을 한 번에 반영했다.
- `articles/market-2026-08-06.html`, `assets/images/articles/market-2026-08-06-ai-split-kospi-pullback-1200x630.webp`, `articles/index.html`, `articles/market.html`, `sitemap.xml`: 외국인·비차익 프로그램 매도와 대형 반도체 집중 하락을 중심으로 8월 6일 장중 시황을 발행 대상으로 추가했다. 당일 미국 AI 공급망 분화와 한국 시장 폭의 괴리를 밝은 색조의 새 대표 이미지에 반영하고 전체 16편·시황 9편, 구조화 데이터와 사이트맵을 동기화했다.
- `assets/data/market-dashboard-20260806-1005.json`, `assets/data/market-dashboard-latest.json`: 발행 시각의 분석·시나리오를 보존하는 불변 스냅샷과 최신 포인터를 추가했다. 대시보드 국내 시세·수급은 독자가 페이지를 열거나 새로고침할 때 최근 응답으로 다시 갱신된다.
- `scripts/update_kodex_volume_pressure.py`, `assets/data/kodex-volume-pressure.json`, `assets/data/kodex-intraday-index.json`, `assets/data/kodex-intraday/*.json`, `.github/workflows/update-kodex-volume-pressure.yml`: KODEX 레버리지의 공개 완료 1분봉을 날짜별로 보존하고 일별 요약과 함께 누적하는 파이프라인으로 확장했다. 첫 관측 봉·종가 단일가·일봉 대비 미포착량은 중립 처리하고 기존 날짜는 명시적 교체 없이 유지한다.
- `assets/market-live-data.js`, `assets/market-dashboard.js`, `assets/market-dashboard.css`, `articles/market.html`, `amplify.yml`: KODEX 가격 차트를 일봉/분봉으로 전환하고 보존된 1분봉을 5·15·30·60분봉으로 묶어 볼 수 있게 했다. 기존의 거의 반반인 매수·매도 적층 막대를 거래량·0선 기준 순압력·CVD로 분리하고, 매수 우위는 빨강, 매도 우위는 파랑으로 표시한다. 날짜별 분봉 JSON도 배포 산출물에 포함했다.
- `assets/market-dashboard.js`, `assets/market-dashboard.css`, `articles/market.html`, `DATA_SOURCES.md`, `RESEARCH_SPEC.md`: 별도 순압력 패널을 없애고 실제 거래량 막대 안에 CVD 변화량의 상대 강도를 빨강 매수 우위·파랑 매도 우위 면적으로 채웠다. 누적 CVD 노란선도 같은 거래량 영역에 겹쳐 거래량 증가와 힘의 방향을 한 번에 읽도록 구성했다.
- `assets/market-dashboard.js`, `assets/market-dashboard.css`, `articles/market.html`: 거래량 X-ray의 라벨 전용 상단 여백을 확보해 최대 거래량 막대와 CVD 선이 글자를 가리지 않도록 했다. 동일 시간의 캔들과 거래량 막대에 같은 선택 인덱스를 부여해 어느 쪽을 마우스로 가리키거나 클릭·키보드 선택해도 두 요소가 함께 강조되도록 연결했다.
- `tests/test_kodex_volume_pressure.py`, `tests/test_kodex_volume_pressure.js`, `DATA_SOURCES.md`, `RESEARCH_SPEC.md`: 분봉별 거래량 합계 보존, Delta·CVD 누적, 날짜별 색인 정렬, 최초 확보분부터 8월 6일 장 마감까지 7거래일 파일 검증과 브라우저 정규화를 추가하고 공개 수치가 체결별 Bid/Ask가 아닌 분봉 기반 통계 추정치임을 명시했다.
- `assets/market-live-data.js`, `assets/market-dashboard.js`, `assets/market-dashboard.css`, `articles/market.html`, `infra/amplify-custom-rules.json`: KODEX 레버리지 전용 화면에 약 3개월 일봉 캔들, 5·20·60일 이동평균과 거래량 차트를 추가했다. 1개월·3개월 전환, 거래일별 OHLC·거래량 읽기, 기간 수익률·고가·저가·누적·일평균 거래량 요약을 제공하고 기존 최근 5거래일 투자자 수급표는 별도 숫자 확인용으로 유지했다. Naver Finance의 종목코드·조회범위를 고정한 읽기 전용 일봉 경로만 Amplify 프록시에 추가했으며, 0거래량 개장 전 임시 일봉과 비정상 OHLC를 차트에 넣지 않도록 검증했다.
- `assets/market-dashboard.js`, `articles/market.html`: 누적·일평균 거래량은 매수·매도 부호가 없는 총거래량이므로 앞에 `+`를 붙이지 않도록 표시 형식을 바로잡고 스크립트 캐시 버전을 갱신했다.
- `DATA_SOURCES.md`, `RESEARCH_SPEC.md`: 3개월 일봉의 15분 브라우저 캐시와 날짜 중복 제거 규칙을 기록했다. 일별 총거래량과 투자자별 순매매는 X-Ray·Bid/Ask 체결량이 아니므로 차트와 수급표를 분리해 표시하도록 고정했다.
- `assets/market-live-data.js`, `assets/market-dashboard.js`, `articles/market.html`: 장 종료 후 다음 거래일 전에 Naver Finance가 종목 `basic`·`integration`의 OHLC·누적 필드를 `-`나 `null`로 초기화하면 종목 응답 전체가 폐기되던 문제를 수정했다. 이 시간대에도 KODEX 종가·등락률·기간 수익률·최근 5거래일 순매매·거래량은 최신 `dealTrendInfos`에서 독립적으로 표시하고, 원천에 없는 고가·저가는 임의로 채우지 않도록 했다.
- 현재 운영 API의 장 종료 후 응답으로 회귀 검사해 KODEX 종가 102,755원, 거래량 19,524,987주, 최근 수급 5거래일이 정규화되고, 비어 있는 OHLC는 완전한 당일 범위로 오인되지 않는 것을 확인했다.
- `articles/market.html`, `assets/market-dashboard.js`, `assets/market-dashboard.css`: 시황 대시보드에 `KODEX 전용` 진입 탭과 `?view=kodex` 공유 URL을 추가했다. 전용 화면에서는 KOSPI·KOSDAQ 개요를 숨기고 KODEX 레버리지 현재가·등락률·당일 OHLC 위치, 누적 거래량·거래대금, 최근 5거래일 투자자별 순매매 수량, 시황 기준선과 KOSPI 수급만 표시하도록 구성했다.
- `assets/market-live-data.js`, `DATA_SOURCES.md`: 기존 Naver Finance 동일출처 응답에서 KODEX 누적 거래량·거래대금, 기간 수익률, 기초지수·운용사·보수와 최근 5거래일 투자자별 순매매 수량을 검증해 정규화했다. 체결별 Bid/Ask·Delta·CVD가 없는 현재 응답으로 X-Ray 비율을 추정하지 않으며, 공개 화면에도 투자자 순매매와 체결 주도 방향을 분리하도록 기록했다.
- `node --check`, `git diff --check`, `scripts/check_worklog_guard.sh`를 통과했고, 1280px 로컬 브라우저에서 전용 URL 진입·개요 숨김·탭 전환·가로 넘침 없음을 확인했다. 720px 이하에서는 4개 탭을 가로 스크롤로 분리해 본문 폭을 침범하지 않게 했다.

## 2026-08-05
- `.agents/skills/kospi-daily-research/SKILL.md`: 조사·검증은 GPT-5.6 Sol Ultra, 공개 원고는 Terra Medium, 최종 퇴고는 Sol Medium으로 분리했다. 기사·차트·이미지를 먼저 완성한 뒤 국내 수치를 한 번의 병렬 요청으로 조회하고 3분 안에 자동 치환·푸시하는 발행 직전 절차를 추가했다.
- `reports/2026-08-05-0929.md`, `STATE.md`, `charts/us_yield_spreads_long_term_2026-08-05.png`, `charts/us_yield_spreads_90d_2026-08-05.png`: 미국 8월 4일 정규장 반도체 급등, AMD 2분기 공식 실적과 시간외 반전, 무역수지·JOLTS·공장주문, 최신 DRAM 현물가와 미 국채 확정치를 8월 5일 개장 초반 리서치로 통합했다. 발행 직전 병렬 조회로 09:27~09:29 KST의 KOSPI·KOSDAQ·삼성전자·SK하이닉스·KODEX 레버리지·USD/KRW·투자자·프로그램 수급을 한 번에 받아 초안 수치를 교체했다.
- `articles/market-2026-08-05.html`, `assets/images/articles/market-2026-08-05-semiconductor-surge-amd-reversal-1200x630.webp`, `articles/index.html`, `articles/market.html`, `sitemap.xml`: 미국 반도체 정규장 급등과 AMD 시간외 -9.08%, 한국 반도체 갭상승과 외국인·비차익 매수를 중심으로 독자용 데일리 브리핑을 발행 대상으로 추가했다. 당일 시황에서 출발한 밝은 서울 아침·반도체 회복 이미지로 새 대표 이미지를 생성하고 전체 15편·시황 8편, 구조화 데이터와 사이트맵을 동기화했다.
- `assets/data/market-dashboard-20260805-0929.json`, `assets/data/market-dashboard-latest.json`: 09:29 KST 불변 스냅샷과 최신 포인터를 추가했다. 대시보드는 발행 당시 분석·시나리오는 보존하고 페이지를 열거나 새로고침할 때 국내 가격·수급을 최신 응답으로 갱신한다.

## 2026-08-04
- `articles/market.html`, `assets/market-dashboard.js`, `assets/market-live-data.js`, `assets/market-dashboard.css`, `RESEARCH_SPEC.md`: 오늘 시황 원고에는 `generatedAt` 기준 작성 시각을 고정 표시하고, 국내 지수·현물 수급·시장 폭·프로그램·KODEX·삼성전자·SK하이닉스·USD/KRW는 페이지를 열거나 다시 볼 때 각 출처의 최근 시각으로 갱신하도록 분리했다. `이전 분석 대비 변화`와 수급·시장 폭·매크로 팩터도 같은 최신 응답으로 동기화하며, 자동 갱신 경로가 없는 메모리·미 국채·일정과 외국인 선물 지연값은 작성 시점 자료임을 표시한다. 장중에는 60초 간격으로 다시 확인하고, 탭에 돌아왔을 때 1분 이상 지났으면 즉시 갱신한다. 개별 종목이나 환율 요청 하나가 실패해도 국내 지수·수급 갱신이 취소되지 않도록 보조 응답을 부분 허용했다.
- 검증: JavaScript 구문·JSON 파싱·diff 공백을 검사하고, 운영 동일출처 시세 API를 연결한 로컬 브라우저에서 KOSPI 6,358.95·정규장 마감 18:59 KST, 외국인 -3,716억원·기관 -5,385억원·프로그램 -4,070억원과 USD/KRW 20:47 KST가 화면에 반영되는지 확인했다. 시황분석 작성 11:05 KST는 고정됐고, 동적 변화 카드·팩터·관찰 기준이 같은 응답으로 바뀌며 가로 넘침과 콘솔 경고·오류가 없었다.
- 후속 검수: KOSDAQ·개별 종목·환율 중 일부 요청이 실패해도 정상 KOSPI·수급을 반영하도록 지수별 부분 성공을 허용하고, 항목별 직전 성공 시각보다 오래된 응답은 적용하지 않도록 보강했다. 부분 지연 상태와 마지막 확인 시각을 공개 문구에 표시하고, 첫 조회 실패·개장 전·장 마감 뒤에도 재시도가 이어지게 했다. 자동 갱신 때 기술적 분석 종목 버튼을 재생성하지 않아 키보드 포커스를 보존하며, 고정 해석에는 11:05 KST 작성 시각을 붙였다. 운영 응답에서 전체 최신값과 KOSDAQ·SK하이닉스 실패를 주입한 부분 응답을 모두 확인했고, 최신 코드로 정적·운영 API 연결 브라우저 QA와 콘솔 오류 0건을 재검증했다.
- `docs/CONTENT_GUIDELINES.md`, `docs/HEALTH_ARTICLE_ROADMAP.md`: 질문형 글의 도입을 `용어 정의 → 유행 배경 → 과장 → 검증 경로`로 고정하던 공식을 폐기했다. 글마다 장면·문제·중심 판단 중 적합한 시작을 고르고, 가상 사례는 첫 등장에 밝히며, 근거 링크는 자료의 의미를 설명한 뒤 연결하도록 정리했다. 공개 원고에서 취재 과정·구조 안내를 제거하고 결론·FAQ·안전 고지는 필요한 위치 한 곳에만 두는 기준도 추가했다.
- `articles/subconscious-hacking-habit-mindfulness.html`, `articles/goal-achievement-behavior-science.html`, `articles/habit-formation-21-days.html`, `articles/positive-affirmations-effect.html`, `articles/index.html`: 건강정보 1~4편을 갱신된 완성 원고 기준으로 다시 퇴고했다. 각 글의 중심 명제를 첫 화면에 제시하고 구성 사례임을 명시했으며, 반복되던 도입 공식·FAQ·결론·시리즈 안내와 취재 과정 문장을 걷어냈다. 연구 결과는 독자에게 필요한 의미가 먼저 나오도록 압축하고, 1편은 자동반응, 2편은 성공담을 시험할 가설, 3편은 반복 기회와 자동성, 4편은 가치 성찰형 자기확언에 집중하도록 역할을 분리했다. 검색·공유 설명과 허브 카드 문구도 새 원고에 맞췄다.
- 검증: 건강정보 1~4편과 허브의 JSON-LD, 목차와 본문 절 수, fragment·중복 ID·로컬 링크, 수정 시각 일치, FAQPage 잔존 여부와 diff 공백을 정적 검사했다. 로컬 브라우저 1280×900과 390×844에서 네 글의 대표 이미지·본문·목차, 모바일 표 내부 스크롤, 문서 가로 넘침 0, 건강정보 필터 4편 노출과 콘솔 경고·오류 0건을 확인했다.
- `.agents/skills/write-finished-manuscript/SKILL.md`, `.agents/skills/write-finished-manuscript/agents/openai.yaml`, `AGENTS.md`, `docs/CONTENT_GUIDELINES.md`: 공개 아티클·칼럼·에세이·대시보드 문구에 적용할 `완성 원고 쓰기` 모드를 추가했다. 취재 노트와 공개 원고를 분리하고, Terra Medium 초고·Sol Medium 퇴고·Sol 고추론 취재 역할을 구분했다. 공개 원고에서는 작성 과정·구조 안내·양비론·관성적 유보·중복 결론과 후속 질문을 제거하고 제목과 완성 본문만 남기도록 고정했다.
- `.agents/skills/kospi-daily-research/SKILL.md`: 시황 리서치의 검증 보고서를 내부 취재 노트로 사용하고, 공개 시황은 새 완성 원고 스킬의 집필·퇴고 단계를 거치도록 발행 계약을 연결했다.
- 검증: 두 프로젝트 스킬을 `quick_validate.py`로 검사했다. Terra Medium은 취재 노트만으로 과정 설명 없는 장면형 칼럼을 작성했고, Sol Medium은 새 사실을 더하지 않은 채 구조 안내·양비론·반복 유보·중복 결론·후속 질문을 제거하는 포워드 테스트를 통과했다.
- `assets/market-live-data.js`, `assets/market-dashboard.js`, `assets/market-dashboard.css`, `articles/market.html`: 대시보드를 열거나 새로고침할 때 KOSPI·KOSDAQ 지수, 국내 투자자·프로그램 수급, 시장 폭, KODEX 레버리지·삼성전자·SK하이닉스의 당일 가격과 USD/KRW 하나은행 고시환율을 최신 거래 시각으로 갱신하도록 구현했다. 최신값은 지수 카드·기관 현물·달러/원·프로그램 수급·기술적 분석의 OHLC 표에 반영하고, 공격·관망·방어 점수와 시나리오·기준선·해설은 발행 당시 분석을 유지한다. 화면 상단에는 시세 기준과 분석 기준을 분리하며, 조회나 검증 실패 시 기존 불변 스냅샷을 계속 표시한다. 좁은 관찰 카드에서도 수급 금액의 단위가 다음 줄로 분리되지 않도록 조정했다.
- `infra/amplify-custom-rules.json`, `DATA_SOURCES.md`, `RESEARCH_SPEC.md`, `.agents/skills/kospi-daily-research/SKILL.md`: Amplify 동일 출처 프록시를 KOSPI·KOSDAQ, 122630·005930·000660의 읽기 전용 `basic`·`integration`과 USD/KRW 하나은행 고시환율 경로로 제한해 기록했다. 응답 코드·거래일·시각·OHLC 범위 검증, 캐시 우회, 9초 타임아웃, 정적 스냅샷 폴백과 시세·분석 시각 분리 원칙을 이후 대시보드 발행 계약에 추가했다.
- `articles/market.html`: 공개 대시보드에 남아 있던 데이터 연결 방식·기준 시각 표시·추천 제외 원칙 설명을 삭제하고, 8월 4일 오전의 반도체·코스닥 온도차와 수급·환율·핵심 가격대를 바로 전하는 시황 문장으로 교체했다. `.agents/skills/kospi-daily-research/SKILL.md`와 `docs/CONTENT_GUIDELINES.md`에는 대시보드 소개·도움말·카드도 독자용 본문과 같은 기준을 적용하도록 보강했다.
- `articles/market.html`, `assets/market-dashboard.css`, `assets/market-dashboard.js`: 시황분석 카테고리의 전용 메인 화면을 KOSPI·KOSDAQ·수급·시장 폭·관찰 기준 대시보드로 만들었다. `시황분석`, `투자전략`, `기술적 분석`을 URL로 공유 가능한 기능 탭으로 구현하고, 조건부 시나리오 전환·브라우저 체크리스트 저장·분석 대상 전환·가격표를 연결했다. 기존 시황 7편은 시황분석 탭의 최신순 아카이브로 모았다.
- `assets/data/market-dashboard-20260804-1051.json`, `assets/data/market-dashboard-latest.json`: 8월 4일 10:49~10:51 KST 검증값을 불변 스냅샷으로 저장하고 최신 포인터를 분리했다. 외국인 선물 지연값에는 별도 기준 시각을 보존했으며, 스트리밍 연결이 없는 화면에는 `실시간` 표현을 쓰지 않았다.
- `articles/index.html`, `articles/market-2026-07-27.html`~`articles/market-2026-08-04.html`, `sitemap.xml`: 시황분석 카테고리와 기존 글의 배지·상단 이동 링크를 새 대시보드와 아카이브로 연결하고, 기존 `?category=market` 방문은 시황분석 화면으로 넘기며 대시보드 URL을 사이트맵에 추가했다.
- `.agents/skills/kospi-daily-research/SKILL.md`, `RESEARCH_SPEC.md`, `STATE.md`: 이후 시황 발행마다 새 스냅샷·최신 포인터·세 기능 탭·기존 글 아카이브를 함께 갱신하는 계약을 추가했다. OHLC를 시간 경로로 연결하거나 추천 순위·승률·RSI 등 검증되지 않은 숫자를 화면용으로 만들지 않도록 고정했다.
- `amplify.yml`: 새 대시보드의 `assets/*.js`, `assets/*.css`가 Amplify 산출물에 포함되도록 배포 패턴을 보강했다.
- 검증: latest 포인터·불변 스냅샷·대응점수·프로그램 수급 산술, JSON·JSON-LD·사이트맵 XML·중복 ID·JavaScript 문법·diff 공백·worklog guard를 정적 검사했다. 로컬 브라우저 1440×1000과 390×844에서 대시보드 조판, 모바일 가로 넘침 0, 세 탭 URL·뒤로가기 구조, 강세 시나리오 전환, 체크리스트 새로고침 보존, SK하이닉스 분석 전환, OHLC 항목 비교와 콘솔 경고·오류 0건을 확인했다.
- `articles/market-2026-08-04.html`, `reports/2026-08-04-1051.md`: 공개 글에 노출된 `신규 사이드카·서킷브레이커가 확인되지 않았다`, `공지를 확인했다`, `새 계약·청산을 찾지 못했다`는 내부 검증 서술을 삭제했다. 메모리 부분은 현물가격·ISM 공급 강세와 기관 비중 축소·선물 헤지라는 독자에게 필요한 시장 해석으로 직접 고쳤다.
- `.agents/skills/kospi-daily-research/SKILL.md`, `docs/CONTENT_GUIDELINES.md`: 공개 글은 조사 일지가 아니라는 편집 원칙을 추가했다. 미발생 사건과 검색·검증·제외 과정은 내부 보고서·상태 문서에만 남기고, 실제 발생이 확인돼 시장 판단에 영향을 주는 사건만 공개 본문에 쓰도록 명시했다.
- `reports/2026-08-04-1051.md`, `STATE.md`: 10:49~10:51 KST 장중값으로 오전 시황을 갱신했다. KOSPI가 1.50% 갭상승 뒤 6,080.25까지 밀렸다가 보합권으로 회복한 경로, 기관 현물 -6,717억원과 외국인 현물 매수·선물 매도, KOSDAQ +5.21%의 극단적 순환매를 핵심 이슈로 정리했다. 미국 기술주 반등·유가와 10년물 하락, ISM이 확인한 메모리 공급 부족을 주가 수급과 분리하고 오늘 신규 헤지펀드 청산·삼성 공급계약이 확인되지 않았다는 점도 명시했다.
- `articles/market-2026-08-04.html`, `articles/index.html`, `sitemap.xml`: 「2026년 8월 4일 코스피 오전 시황: 6,080 찍고 보합, 코스닥 5% 급등」을 최신 시황분석으로 발행 대상으로 추가했다. 장중 기준 시각을 독자용 문장으로 밝히고 외국인·기관·프로그램 수급, 시장 폭, 메모리 현물, 물가·환율·국채, 오후 조건부 시나리오를 한 글에 연결했다. 아티클 허브를 전체 14편·시황 7편으로 바로잡고 ItemList·대표 이미지·사이트맵을 동기화했다.
- `assets/images/articles/market-2026-08-04-broad-rebound-memory-lag-1200x630.webp`: 과거 썸네일을 참조하지 않고 오늘의 급락 후 회복과 대형 반도체 소외를 표현하는 새 이미지를 생성했다. 밝은 하늘과 청록색 회복 경로, 금빛으로 확산되는 시장 폭, 낮은 길에 남은 두 메모리 칩을 사용해 1200×630 WebP로 연결했다.
- 검증: 신규 글의 Article·Breadcrumb JSON-LD, 목차 fragment·중복 ID·로컬 링크, 사이트맵 XML, 허브 ItemList 14개·연속 position·카드 14개·시황 7개·단일 고우선 이미지를 정적 검사했다. 로컬 브라우저 1280×900과 390×844에서 상세 글·목록·시황 필터, 새 썸네일과 기존 카드의 동일 크기, 모바일 표 내부 가로 스크롤, 문서 가로 넘침 0과 콘솔 경고·오류 0건을 확인했다.
- `articles/subconscious-hacking-habit-mindfulness.html`, `articles/goal-achievement-behavior-science.html`, `articles/habit-formation-21-days.html`, `articles/positive-affirmations-effect.html`: 건강정보 1~4편의 도입을 `용어의 뜻 → 유행한 배경 → 과장된 약속 → 핵심 질문 → 중심 사례와 확인 경로`로 다시 구성했다. 연구 결론과 실천 절차부터 꺼내던 기존 도입을 독자의 출발점에서 읽히도록 바꾸고, 도입과 첫 절의 설명이 겹치던 2편은 온라인 강의 첫 주 장면으로 첫 절을 다시 열었다. 네 글의 검색·공유 설명과 수정 시각도 본문 질문에 맞췄다.
- `articles/index.html`, `sitemap.xml`: 건강정보 1~4편 카드 소개를 각 글의 배경과 핵심 질문이 바로 보이도록 고치고, 허브와 네 글의 사이트맵 수정일을 갱신했다.
- `docs/CONTENT_GUIDELINES.md`, `docs/HEALTH_ARTICLE_ROADMAP.md`: 이후 질문형 건강정보 글도 독자가 용어나 논쟁을 안다고 가정하지 않고, 연구를 언급하기 전에 뜻·유행 맥락·과장을 설명하도록 도입 편집 기준을 추가했다. 중심 사례는 도입에서 예고하고 첫 절에서 구체화해 같은 내용을 반복하지 않도록 정했다.
- 검증: 네 글과 허브의 HTML·JSON-LD, 로컬 링크·목차 fragment·중복 ID, 수정 시각 일치, 건강정보 카드 4편과 사이트맵 XML을 정적 검사했다. 로컬 브라우저 1280×900과 390×844에서 네 도입부의 조판·대표 이미지 로딩·문서 가로 넘침 0, 건강정보 필터 4편 노출과 콘솔 경고·오류 0건을 확인했다.

## 2026-08-03
- `reports/2026-08-03-1430.md`, `STATE.md`: 14:30 KST 한국장 장중값을 새로 검증해 KOSPI -4.76%, 삼성전자 -8.19%, SK하이닉스 -7.51%, 외국인 현물 -2조5,489억원, 비차익 프로그램 -1조9,747억원과 달러/원 약 1,430원을 기록했다. Micron의 금요일 12.9%p 장중 급반전, 유가 하락, Amazon·Apple 실적, AI 헤지펀드 공개주식 북 매각, 강한 DRAM 현물가를 이슈 우선순위로 통합하고 실물 공급 부족과 주식 디레버리징을 분리했다.
- `articles/market-2026-08-03.html`, `articles/index.html`, `sitemap.xml`: 「2026년 8월 3일 코스피 장중 급락: 원화 강세에도 반도체 매도 집중」을 시황분석 최신 글로 추가했다. 장중 시세·수급, 미국 정규장, FRED 확정치를 세션별로 구분하고 독자가 읽는 본문에는 차트 제작 과정이나 내부 설명을 노출하지 않았다. 아티클 허브 ItemList를 13편으로 갱신하고 새 글·대표 이미지·금리차 차트를 사이트맵에 연결했다.
- `assets/images/articles/market-2026-08-03-semiconductor-deleveraging-1200x630.webp`: 과거 썸네일을 참조하지 않고 당일 시황을 반영해 새 대표 이미지를 생성했다. 밝은 낮빛 아래 견고한 두 메모리 칩에서 푸른 자금 블록이 빠져나오고, 원화 강세·유가 하락을 상징하는 청록색 완충망이 이를 받치는 장면으로 구성했다. 1200×630 WebP로 변환하고 목록 카드와 Open Graph에 연결했다.
- `charts/us_yield_spreads_long_term_2026-08-03.png`, `charts/us_yield_spreads_90d_2026-08-03.png`: FRED 7월 31일 최신 확정치인 10Y-2Y +0.47%p, 10Y-3M +0.92%p를 장기 추세와 최근 90일 확대 차트에 반영했다. 최근값 라벨은 독자가 두 수준과 확대 방향을 바로 읽을 수 있도록 분리 배치했다.
- `articles/market-2026-08-03.html`: 자료 기준 문단의 내부 작업 표현인 `14:30 KST에 동결했습니다`를 걷어내고, 독자가 출처와 기준 시각을 자연스럽게 이해하도록 `8월 3일 14:30 KST 기준의 KRX 장중 데이터를 반영했습니다`로 고쳤다.

## 2026-08-02
- `articles/positive-affirmations-effect.html`, `assets/images/articles/positive-affirmations-effect-1200x630.webp`: 건강정보 전체 목차 4편 「긍정 확언은 효과가 있을까?」를 추가했다. 검색 상위 글의 장면 중심 전개와 기존 1~3편의 연구 나열 구조를 대조해, 발표 10분 전 한 장면이 `마음속 반박 → 가치 성찰 → 세 줄 작성 → 작은 기대 효과 → 실제 행동 확인`으로 이어지게 구성했다. Wood 연구와 재현 실험, 2014년 리뷰, 2015·2025년 메타분석의 의미를 링크 가까이 일상어로 설명하고, 글자·로고 없는 1200×630 WebP 대표 이미지를 연결했다.
- `articles/index.html`, `articles/subconscious-hacking-habit-mindfulness.html`, `sitemap.xml`: 신규 글을 허브 최신 카드와 ItemList 1번에 넣고 공개 아티클 12편·건강정보 4편으로 동기화했다. 1편의 긍정 확언 연구 설명에서 4편으로 이어지는 내부 링크를 추가하고, 허브 대표 이미지·로딩 우선순위·사이트맵 글과 이미지 URL을 갱신했다.
- `docs/CONTENT_GUIDELINES.md`, `docs/HEALTH_ARTICLE_ROADMAP.md`: 핵심 질문을 도입 세 문단 안에서 답하고, 본문 절 5~6개가 한 장면의 다음 질문으로 이어지며, 글 절반 전 실천 예시를 제시하도록 문장·서사 기준을 보강했다. 로드맵에는 `HC-004 published / PUB-005`, 근거 자료팩, 공개 내부 링크와 `HC-005`부터의 다음 작성 큐를 기록했다.
- 검증: 신규 글의 JSON-LD·화면 FAQ·인용 링크·목차 fragment·로컬 링크·중복 ID, 허브 ItemList 12개·건강정보 4개·순차 position·단일 고우선 이미지, 사이트맵 XML과 대표 이미지 1200×630 규격을 정적 검사했다. 본문은 FAQ를 제외한 핵심 절 6개, 문단 25개, 문단당 평균 103자·최장 162자로 확인했다. 로컬 브라우저의 1265px 데스크톱과 375px 모바일에서 상세 글·대표 이미지·목차·세 줄 실천법과 건강정보 필터 4편을 확인했으며 가로 넘침과 콘솔 경고·오류는 0건이었다.
- `articles/habit-formation-21-days.html`, `assets/images/articles/habit-formation-21-days-1200x630.webp`: 건강정보 전체 목차 3편 「습관은 정말 21일이면 만들어질까?」를 발행 대상으로 추가했다. 점심 뒤 10분 걷기 한 장면으로 21일·66일 수치의 의미, 같은 기간에도 달라지는 신호·행동 반복 횟수, 자동성과 실제 행동량의 차이, 하루를 놓친 뒤 다시 이어가는 방법을 설명했다. 2024년 체계적 문헌고찰과 Lally·Keller·Ebert 연구, NIDDK 안내를 링크만 열지 않아도 뜻이 이어지도록 풀어 썼으며 글자·숫자·로고 없는 1200×630 WebP 대표 이미지를 연결했다.
- `articles/index.html`, `sitemap.xml`: 신규 글을 최신 카드와 ItemList 1번에 추가하고 공개 아티클 11편·건강정보 3편으로 수와 순서를 동기화했다. 허브 대표 이미지, 최신 이미지 로딩 우선순위, 사이트맵의 글·이미지 URL과 수정일도 함께 갱신했다.
- `articles/subconscious-hacking-habit-mindfulness.html`, `articles/goal-achievement-behavior-science.html`, `docs/HEALTH_ARTICLE_ROADMAP.md`: 공개 1·2편에서 3편으로 이어지는 내부 링크를 추가하고 수정 시각을 맞췄다. 로드맵에는 `HC-003 published / PUB-004`, 사용 근거, 공개 링크 맵을 기록하고 다음 작성 큐를 `HC-004`부터 다시 정렬했다.
- 검증: 신규 글의 JSON-LD·화면 FAQ·인용 링크·목차 fragment·로컬 링크·중복 ID와 허브 ItemList 11개·건강정보 3개·사이트맵 XML·대표 이미지 1200×630 규격을 정적 검사했다. 로컬 브라우저 1280×720과 390×844에서 본문·대표 이미지·목차·표를 확인했으며, 모바일 표는 341px 영역 안에서 가로 스크롤되고 문서 가로 넘침과 콘솔 경고·오류는 0건이었다. 건강정보 필터에는 공개 글 3편만 최신순으로 노출됐다.

## 2026-07-31
- `articles/subconscious-hacking-habit-mindfulness.html`: 건강정보 1편을 `일이 막힌 순간 울린 휴대전화 알림` 한 장면으로 다시 구성했다. 잠재의식 용어 설명을 `신호 → 첫 생각 → 주의 → 행동과 결과` 네 단계에 통합하고, 회의 사례·자동적 사고 표·실행의도 목록·7일 관찰표를 삭제했다. 습관 신호를 일상의 반복 장면으로 풀어 쓰고, 행동이 저절로 시작되는 느낌과 실제 행동 횟수를 따로 관찰한다는 관점을 중심에 두었다. 마지막에는 점수식 양식 대신 오늘 한 장면을 자연어로 기록하는 문장과 2편의 행동 계획으로 이어지는 링크를 남겼다.
- `articles/goal-achievement-behavior-science.html`: 건강정보 2편을 온라인 강의 한 주의 이야기로 다시 구성했다. 성공담 점검 목록, 기본·대체·최소 행동 카드, 환경 준비 목록, 실행·변화 기록 목록, 유지·수정·중단 표를 본문 서사에 흡수하고 마지막 9행 계획표만 남겼다. 월·금 실행, 수요일 야근, 목요일 아침 대체 계획 실패, 완료 단원 `3개 → 5개`, 다음 주 토요일 오전으로 이동하는 기록을 연결해 계획에서 틀린 조건 하나를 고치는 과정을 보여 주었다. 걷기 자동성 연구는 1편에만 남기고 2편에서는 내부 링크로 연결해 두 글의 역할을 `반응 관찰`과 `행동 설계·주간 수정`으로 분리했다.
- `sitemap.xml`: 두 건강정보 글의 `lastmod`를 2026-07-31로 갱신했다.
- 검증: 두 글의 JSON-LD 파싱, 화면 FAQ와 FAQPage 문구 일치, 목차 fragment, 중복 ID, 로컬 링크, 화면 출처와 Article citation, 수정 시각 동기화, 사이트맵 XML과 diff 공백을 정적 검사했다. 로컬 브라우저 1280×720과 390×844에서 대표 이미지·본문 폭·목차, 1편의 단일 네 단계 카드, 2편의 단일 계획표를 확인했다. 문서 가로 넘침과 콘솔 경고·오류는 0건이었고, 모바일 계획표만 341px 영역 안에서 580px 폭으로 가로 스크롤되는 것을 확인했다.

## 2026-07-30
- `articles/subconscious-hacking-habit-mindfulness.html`, `articles/goal-achievement-behavior-science.html`: 건강정보 1·2편의 문장을 초독자가 한 번에 뜻을 잡을 수 있는 장면·행동 중심 문장으로 다시 다듬었다. 1편은 휴대전화 알림 장면의 `신호 → 첫 생각 → 주의 → 행동`을 끝까지 유지하고, 겹치던 설명과 추상 표현을 줄였다. 2편은 도입부의 절차 목록을 온라인 강의 7일 실험 이야기로 바꾸고, `과정지표·결과지표`를 `실행 기록·변화 기록`으로 풀어 썼다. 두 글 모두 연구가 뜻하는 바를 먼저 설명한 뒤 링크를 연결하고, 자동성의 느낌과 실제 행동량, 계획에서 틀린 조건을 찾아 고치는 주간 검토라는 심화 관점은 유지했다.
- 검증: 두 글의 JSON-LD 파싱, 화면 FAQ와 FAQPage 문구 일치, 목차 fragment, 중복 ID, 로컬 링크, 수정 시각 동기화, 내부 편집 코드·분량 추정 읽기 시간·이전 용어 잔존 여부를 정적 검사했다. 로컬 브라우저 1440×1000·390×844에서 대표 이미지, 본문 폭, 카드 열, 표 내부 가로 스크롤, 문서 가로 넘침 0과 콘솔 경고·오류 0건을 확인했다.
- `articles/subconscious-hacking-habit-mindfulness.html`, `articles/goal-achievement-behavior-science.html`: 건강정보 1·2편을 문장 교정이 아닌 독자 서사 기준으로 전면 재편집했다. 1편은 휴대전화 자동반응 장면을 `신호 → 첫 해석 → 주의 → 행동`으로 이어가고, 잠재의식이라는 큰 질문을 관찰 가능한 단위로 좁혔다. 자동성이 커지는 느낌과 실제 행동량을 분리해 설명하고, 7일 표는 매일 같은 다섯 항목으로 기준선과 변경 후를 비교하도록 고쳤다. 2편은 온라인 강의 사례 하나로 성공담 선별, 이번 주 행동, 시작 신호, 실행·결과 기록, 주간 검토까지 연결했다. 성공담에서 행동을 골라 시험하고 틀린 가정을 한 주 단위로 고치는 관점을 중심에 두었으며 중복 표·체크리스트·돌출 용어와 내부 `HC-###` 표기를 제거했다.
- `articles/index.html`, `docs/CONTENT_GUIDELINES.md`, `docs/HEALTH_ARTICLE_ROADMAP.md`: 건강정보 카드 소개를 두 글의 실제 독자 질문과 중심 사례에 맞게 고쳤다. 이후 글도 구체적인 장면으로 시작하고, 글 전체에서 같은 사례와 분류를 유지하며, 연구의 의미를 표본·통계보다 먼저 설명하고, 공개 본문에서 내부 편집 상태를 노출하지 않도록 문장·서사 규칙을 추가했다.
- 검증: 두 글의 JSON-LD 파싱, 화면 FAQ 4개와 FAQPage 문구 일치, 목차 fragment, 중복 ID, 로컬 링크, 수정 시각 동기화, 사이트맵 XML과 diff 공백을 정적 검사했다. 근거·문체·구조 교차 재검수에서 미해결 P0/P1은 없었다. 로컬 브라우저 1280×800·390×844에서 대표 이미지·본문 폭·목차·표 내부 스크롤·건강정보 필터 2개를 확인했으며 문서 가로 넘침과 콘솔 경고·오류는 0건이었다.
- `articles/index.html`, `articles/*.html`, `docs/drafts/health/HC-026-how-to-meditate-5-minute-breathing.draft.html`: 아티클 목록과 각 글 상단 메타데이터의 수동 `약 N분` 표기를 모두 제거했다. 이 값은 실제 체류·시황 갱신 시간과 무관한 본문 분량 추정치여서 독자 판단에 도움이 되지 않는다는 기준을 적용했다.
- `docs/CONTENT_GUIDELINES.md`: 이후 새 글과 초안에도 본문 분량 추정 읽기 시간을 공개 메타데이터로 넣지 않도록 운영 기준을 추가했다.

## 2026-07-28
- `articles/subconscious-hacking-habit-mindfulness.html`, `articles/goal-achievement-behavior-science.html`: 건강정보 1·2편의 근거 링크 앞뒤를 `독자의 질문 → 연구 대상·방법 → 일상어 결과 → 현재 문단의 결론·다음 행동` 순서로 다시 썼다. 링크를 열지 않아도 연구가 무엇을 비교했고 본문에서 왜 인용했는지 이해되도록 설명했으며, 2편의 `d`, `95% CI` 같은 설명 없는 통계 약어는 쉬운 결과 표현으로 교체했다. 1편은 확언 연구의 2020년 재현 실패 자료를 추가하고 반복되던 방어형 대조 문장을 줄였다.
- `docs/HEALTH_ARTICLE_ROADMAP.md`: 이후 글에서도 본문 근거 링크를 독립적으로 이해할 수 있게 설명하고, 효과크기·신뢰구간 약어는 일상어 의미를 먼저 제시하도록 편집 원칙을 추가했다. 두 글의 JSON-LD·화면 FAQ 일치, 목차 fragment, 중복 ID, 사이트맵 XML과 diff 공백을 정적 검사했으며 로컬 브라우저 데스크톱·모바일에서 가로 넘침 0, 대표 영역 조판과 콘솔 경고·오류 0건을 확인했다. 근거·문체·구조 교차 재검수에서 미해결 P0/P1은 없었다.
- `docs/HEALTH_ARTICLE_ROADMAP.md`: 전체 목차 번호를 고정 콘텐츠 ID `HC-001`~`HC-032`로 통일하고, 실제 배포 이력 `PUB-###`·작성 큐·상태를 별도 필드로 분리했다. 독자에게 보이는 `연재 n편`은 HC 번호만 뜻하도록 규칙을 명시했으며, 전체 목차 2편을 `HC-002 published`, 명상 글을 `HC-026 private-draft`로 바로잡았다.
- `articles/goal-achievement-behavior-science.html`, `assets/images/articles/goal-achievement-behavior-science-1200x630.webp`: 건강정보 연재 `HC-002` 「목표 달성 방법: 성공 법칙을 행동과학으로 바꾸는 4단계」를 추가했다. 목표 설정·실행의도·환경·진행 기록·피드백을 15분 계획표와 7일 실험으로 구성하고, 목표 설정 및 모니터링 메타분석, 안정된 맥락 RCT, 선별된 성공 사례와 생존자 편향 연구를 원문 링크로 연결했다. 글자·로고 없는 1200×630 WebP 대표 이미지를 제작해 Article·Open Graph·Twitter 데이터와 동기화했다.
- `docs/drafts/health/HC-026-how-to-meditate-5-minute-breathing.draft.html`, `docs/drafts/health/assets/how-to-meditate-5-minute-breathing-1200x630.webp`: 먼저 공개됐던 명상 글과 대표 이미지를 Amplify artifact 경로에서 제거해 비공개 초안으로 이동했다. 초안 HTML에는 `noindex,nofollow,noarchive`와 재검토 주석을 추가했으며, `HC-026` 작성 시 원래 공개 경로로 복원할 절차를 README에 기록했다.
- `articles/index.html`, `articles/subconscious-hacking-habit-mindfulness.html`, `sitemap.xml`: 허브 최상단 카드·대표 이미지·ItemList와 사이트맵을 `HC-002`로 교체하고 전체 5개·건강정보 2개 수를 유지했다. 연재 1편의 실행의도·연재 안내는 2편과 왕복 연결하고, 비공개 `HC-026` 링크는 모두 제거했다. 카드의 `display:grid`가 `hidden` 속성을 덮어 카테고리 필터에서 다른 글까지 보이던 기존 문제는 비대상 카드의 인라인 `display:none`을 함께 적용해 수정했다.
- 검증: 라이브 대상 22개 HTML의 로컬 링크·fragment·중복 ID, 16개 JSON-LD, 화면 FAQ와 FAQPage, 사이트맵 XML, 허브 5개·건강정보 2개·최신순·로딩 우선순위, 신규·초안 이미지 1200×630 규격과 명상 live artifact 제거를 정적 검사했다. 로컬 브라우저 1280×720·360×800에서 새 글 이미지·목차·시나리오 카드·모바일 표 스크롤·가로 넘침 0, 건강정보 필터 2개 노출, 명상 URL 404와 콘솔 경고·오류 0건을 확인했다. 근거·SEO·저장소 교차 리뷰의 P1 1건을 연구 범위에 맞게 수정한 뒤 미해결 P0/P1이 없음을 재확인했다.
- 이전 배포 이력(현재 비공개): `articles/how-to-meditate-5-minute-breathing.html`, `assets/images/articles/how-to-meditate-5-minute-breathing-1200x630.webp`에 명상 초안을 공개했다. 이후 전체 목차 `HC-026`으로 분류해 `docs/drafts/health/`로 이동했다.
- 이전 배포 이력(현재 교체됨): 명상 초안을 허브·사이트맵·건강정보 1편에 연결했으나, 현재 연결은 전체 목차 `HC-002`로 모두 교체했다.
- 검증: 근거·SEO·저장소 통합 리뷰에서 지적된 즉시 의료 평가 조건과 전문가 미검수 고지를 보완한 뒤 미해결 P0/P1이 없음을 재확인했다. 22개 HTML의 로컬 링크·fragment·중복 ID, 16개 JSON-LD 블록, 화면 FAQ와 FAQPage 일치, 사이트맵 XML, 허브 수·최신순, 방어형 대조 표현 0건과 대표 이미지 1200×630 규격을 정적 검사했다. 로컬 브라우저 1280×720·360×800에서 상세 글과 건강정보 필터 2개 노출, 이미지 로딩, 모바일 표 내부 가로 스크롤, 문서 가로 넘침 0과 콘솔 오류 0건을 확인했다.
- `articles/market-2026-07-28.html`, `docs/CONTENT_GUIDELINES.md`: 시황 글에 반복된 `A가 아니라 B다`·`단정할 수 없다` 식 방어형 문장을 걷어냈다. 수치와 원인은 직접 서술하고, 장중 스냅샷·투자 비권유·해석의 범위는 상단 고지와 필요한 한 곳에만 남겼다. FAQ도 오해 반박형 질문 대신 시장 작동 방식과 오후 확인 지표를 바로 설명하도록 바꾸고, 이후 아티클의 같은 문체 반복을 막는 내부 기준을 추가했다.
- 검증: 시황 글의 구조화 데이터·FAQ 화면 텍스트 일치, 대조형 부정 표현 0건, 로컬 1280px·360px 화면의 본문 흐름과 가로 넘침 0을 확인했다.
- `articles/market-2026-07-28.html`: 2026년 7월 28일 09:29~09:30 KST 기준 코스피 장중 급락 분석을 게시 글로 추가했다. 코스피·코스닥과 삼성전자·SK하이닉스 가격, 외국인·기관·프로그램 수급, 상승·하락 종목 수를 고정 시각 스냅샷으로 구분하고 미국 증시 전체 혼조와 반도체 약세, CXMT 상장, 원화 약세가 국내 수급과 맞물린 흐름을 분석했다. 코스피·코스닥 매도 사이드카와 1단계 서킷브레이커의 조건·효과를 한국거래소 기준으로 나누고, 장중 수치의 변동 가능성·투자 비권유·개인 자문이 아니라는 한계를 명시했다.
- `articles/index.html`, `sitemap.xml`, `assets/images/articles/market-2026-07-28-selloff-sidecar-1200x630.webp`: 새 시황을 아티클 허브 최상단과 시황분석 필터에 연결하고 ItemList 수·순서, Open Graph·Twitter 대표 이미지, 이미지 사이트맵과 수정일을 동기화했다. 급락과 시장 안정 장치를 상징하는 글자·숫자·로고 없는 1200×630 WebP 대표 이미지를 제작해 상세 글과 허브에 적용했다.
- 검증: Article·BreadcrumbList·FAQPage 및 허브 ItemList JSON-LD, 사이트맵 XML, 로컬 링크·fragment·중복 ID, FAQ 화면·구조화 데이터 일치, 대표 이미지 1200×630 규격, 카테고리 수와 최신순 노출을 정적 검사했다. 로컬 브라우저 1280px·360px에서 상세 글·허브·시황분석 필터의 이미지 로딩, 모바일 표 가로 스크롤, 목차 대상, 가로 넘침 0과 필터 실행을 확인했다.

## 2026-07-27
- `index.html`: 홈 첫 문구를 `연마의 원리와 한계.`로 줄이고, 상세 설명은 호흡과 그립 트레이닝 등의 도구로 현재 상태를 측정·기록한다는 한 문장으로 정리했다. 검색·Open Graph·Twitter 설명도 같은 범위로 간결하게 맞췄다.
- `index.html`, `app.html`, `articles/*.html`, `guides/*.html`, `style.css`: 공통 헤더의 `연마` 제목을 왼쪽 아이콘과 동일한 홈 주소로 연결했다. 기존 제목 위치와 문서의 h1 구조를 유지하면서 44px 클릭 영역, 키보드 포커스와 한영 접근성 이름을 적용하고 해당 페이지의 스타일 캐시를 `20260727-5`로 갱신했다.
- 검증: 공통 헤더 13개의 아이콘·제목 홈 경로 일치, 전체 20개 HTML의 로컬 링크·fragment·중복 ID, JavaScript 구문과 diff 공백을 검사했다. 로컬 브라우저에서 홈 문구·44px 제목 링크·가로 넘침 0·콘솔 오류 0건을 확인하고, 아티클 하위 경로의 `연마` 클릭이 실제 루트로 이동하는지와 앱 영문 전환 시 `Yeonma`/`Go to the Yeonma home page`가 적용되면서 h1이 유지되는지 확인했다.
- `style.css`, 공통 HTML: Astryx의 시각 원리를 연마의 정적 HTML 구조에 맞춰 단계적으로 적용했다. Graphite 계열 표면·텍스트·경계·브랜드·상태·간격·반경·모션 토큰을 추가하고, 마케팅 본문 1200px·읽기 본문 800px·조작부 최소 44px 계약을 공통화했다. 데스크톱 사이트 헤더는 고정하되 의미상 중첩된 `header`에는 전파되지 않도록 `body > header`로 범위를 제한했고, 1040px 이하에서는 정적 헤더로 전환했다. React·StyleX를 새로 도입하지 않고 기존 정적 사이트와 캐시 구조를 유지했으며 전체 스타일 참조를 `20260727-4`로 갱신했다.
- `index.html`, `style.css`: 홈의 장문 카드 나열을 아브라행·호흡의 실제 인터페이스를 축약한 2개 대표 쇼케이스와 발 핏·CO2·시퀀스·로또의 4개 압축 유틸리티 행으로 재구성했다. 기능 목적·주요 측정값·한계를 첫 화면에서 비교할 수 있게 하고 1024px·720px 이하에서 순차적으로 단일 열로 전환한다.
- `app.html`, `abrahang-timer.js`, `i18n.js`, `style.css`: 아브라행 링 상단을 현재 하중·피크 하중·현재 손·저울 연결의 4지표 관찰 콘솔로 확장했다. 긴 Bluetooth 진단문은 설정 패널에 유지하고 콘솔에는 `미연결/연결 중/수신 중/확인 필요` 축약 상태만 표시해 레이아웃 점프와 반복 라이브 공지를 막았다. 맥스행 휴식 본구간은 `회복`, 마지막 3초부터 다음 손을 표시해 실제 손 전환 알림 시점과 맞췄다. 1040px 이하 콘솔은 2×2, 420px 이하 요약도 2×2로 전환하고 저울·Chrome flags·히스토리 버튼을 44px 이상으로 통일했다. Bluetooth 파싱·측정 수식·프로토콜 식별자와 기존 저장 데이터 계약은 변경하지 않았으며 번역·아브라행 스크립트 캐시는 `20260727-3`·`20260727-2`로 갱신했다.
- `articles/*.html`, `guides/*.html`, `style.css`: 아티클과 가이드에 공통 읽기 셸, 본문 바로가기, 상단 이동, 800px 읽기 폭, 목차와 섹션 앵커를 적용했다. 데스크톱 고정 헤더 높이만큼 앵커 여백을 두고 모바일에서는 이를 해제했으며, 목차·상단 이동·아티클 카테고리 링크의 터치 영역을 44px 이상으로 보강했다. 아티클 허브는 최신 글을 강조하는 피드 구조를 유지하면서 카드 위계와 카테고리 필터를 정돈했다.
- 검증: 전문 에이전트의 기능·정보구조·시각·접근성 회귀 리뷰에서 공통 `header` 범위, 고정 헤더 목차 가림, 긴 Bluetooth 상태문과 영문 중간 폭 충돌을 찾아 수정한 뒤 미해결 P0/P1이 없음을 재확인했다. JavaScript 3개 구문, staged/unstaged diff 공백, 20개 HTML의 중복 ID·로컬 링크·fragment·JSON-LD, sitemap XML, 460개 i18n 참조를 정적 검사했다. 로컬 브라우저 1280px에서 홈 2개 쇼케이스·4개 유틸리티, 아브라행 한영 콘솔·맥스행 결과 구조, 최신순 아티클 3개·건강정보 상세·아브라행 가이드, 목차 앵커 여백, 가로 넘침 0과 콘솔 오류 0건을 확인했으며 1040·560·420px 규칙은 별도 정적 반응형 감사를 통과했다. 실제 Bluetooth 기기 수신은 이번 디자인 변경에서 다시 수행하지 않았다.
- `docs/HEALTH_ARTICLE_ROADMAP.md`: 의식·잠재의식, 우주적 진리, 동서양 철학, 에너지·영성, 명상 수련이라는 첨부 자료의 다섯 축을 종교 권유나 치료 주장 없이 다루는 32편 건강정보 SEO 로드맵을 작성했다. 우선 발행 순서, 검색 의도, 주장별 검증 기준, 첫 글의 APA·NIMH·NIH·WHO·NCCIH·동료평가 기초자료와 내부 링크 운영 원칙을 정리했다.
- `articles/subconscious-hacking-habit-mindfulness.html`, `articles/index.html`, `sitemap.xml`, `assets/images/articles/subconscious-hacking-habit-mindfulness-1200x630.webp`: 건강정보 첫 글 「잠재의식 해킹은 가능할까? 자동적 사고와 습관의 과학」을 발행 대상으로 추가했다. 잠재의식을 자동적 사고·습관 자동성·주의 조절로 구분하고 21일 법칙, 확언, 잠재음원, 마음챙김의 근거와 한계, 7일 자기관찰법, 중단·상담 조건을 원문 링크와 함께 작성했다. canonical·Open Graph·Twitter·Article/Breadcrumb/FAQ 구조화 데이터, 건강정보 허브 카드·필터 수·ItemList, 이미지 사이트맵을 동기화하고 글자·로고 없는 1200×630 WebP 대표 이미지를 연결했다.
- `assets/images/articles/*.webp`: 에세이와 시황분석의 검색·공유 대표 이미지 2장, 시황의 종목 집중과 분산을 설명하는 본문 이미지 1장을 글자·로고 없이 실물 스틸라이프 방식으로 제작했다. 대표 이미지는 1200×630, 본문 이미지는 1200×800으로 맞추고 WebP 품질 80으로 변환해 각각 약 34KB·39KB·109KB로 최적화했다.
- `articles/index.html`, `articles/gen-z-last-generation.html`, `articles/market-2026-07-27.html`, `style.css`: 공개 아티클 허브의 내부 운영용 `편집 기준` 섹션과 관련 스타일을 제거했다. 허브 카드, 두 글의 상단 대표 이미지, 시황의 리밸런싱 설명 이미지를 반응형으로 추가하고 실제 이미지 내용에 맞춘 대체 텍스트·캡션, 고정 크기, 상단 우선 로딩과 본문 지연 로딩을 적용했다. 세 페이지의 스타일 캐시 버전은 `20260727-3`으로 통일했다.
- `articles/*.html`, `sitemap.xml`, `amplify.yml`: 대표 이미지를 Open Graph·Twitter 대형 카드·JSON-LD `ImageObject`에 절대 URL로 연결하고 `max-image-preview:large`를 허용했다. 이미지 사이트맵 네임스페이스와 이미지 URL 3개를 추가했으며 Amplify 산출물 허용 목록에 `assets/images/articles/*.webp`를 포함했다.
- 검증: WebP의 실제 크기·용량, 세 페이지 JSON-LD와 이미지 사이트맵 XML 구문, 모든 로컬 이미지 경로·대체 텍스트·로딩 속성을 정적 검사했다. 로컬 Chrome에서 1440×900과 360×800 화면의 허브 카드·대표 이미지·본문 이미지, 이미지 로딩, 가로 넘침 없음, 콘솔 오류 0건을 확인했다.
- `articles/market-2026-07-27.html`, `articles/index.html`, `style.css`, `sitemap.xml`: 2026년 7월 27일 10:16 KST 기준 시황분석 글을 추가했다. 공유 원고의 반도체 장기 호재·미국 기술주 조정·코스닥 상대 강세·환율·금리·주간 일정·오후 시나리오 논지를 유지하면서 발표 규모와 장중 데이터의 기준 시각을 분리하고, 기업 IR·금융위원회·FRED·미국 정부 일정으로 핵심 수치와 일정을 대조했다. 아티클 허브에는 시황분석을 최신 글로 우선 노출하고 카테고리 수·구조화 데이터·사이트맵을 갱신했으며, 모바일 가로 스크롤 표와 시장 스냅샷·시나리오 카드를 추가했다.
- `about.html`, `index.html`, 공통 내비게이션, `docs/CONTENT_GUIDELINES.md`: 공개 `소개·작성·검증 정책`을 운영 주체·제공 콘텐츠·이용 한계·문의 방법만 남긴 `연마 소개`로 축약하고, 홈의 상세 검수 원칙·변경 이력 패널도 제거했다. 문서 제작 절차, 자동화 도구 원칙, 상세 안전·정정 기준은 Amplify 배포 대상이 아닌 내부 운영 문서로 옮기고, 사이트 전반의 `소개·검증`·`작성·검증 정책` 링크 문구를 `소개`로 통일했다. 각 가이드의 작성 주체·최종 검증일·출처 표시는 유지했다.
- `articles/gen-z-last-generation.html`: 공개 글의 상단 고지, 출처 목록, 구조화 데이터에서 원고 제공 경로에 관한 표기를 제거했다. 통계 자료를 별도로 대조했다는 사실과 인과관계 해석이 필자의 관점이라는 편집 고지는 유지했다.
- `articles/index.html`, `style.css`: 연마의 새 아티클 허브를 추가했다. `전체·에세이·시황분석·건강정보` 카테고리를 URL query로 구분하고, 정적 HTML 카드와 발행일 기준 정렬 스크립트를 함께 사용해 JavaScript가 없어도 글이 노출되면서 실행 시에는 항상 최신 글이 먼저 오도록 구성했다. 카테고리 링크는 현재 상태와 글 수를 표시하며, 글이 없는 카테고리에는 빈 상태 안내를 제공한다.
- `articles/gen-z-last-generation.html`: 사용자가 제공한 원고의 55개 본문 단락 중 통계 문단을 제외한 54개를 그대로 보존하고, 통계 문단은 출처를 확인할 수 있는 표현으로 다듬어 에세이 「인류의 마지막 세대가 될지도 모르는 Z세대」로 편집했다. 장문 전용 72자 폭, 목차, 카테고리·발행일·읽기시간·의견 글 표시, 편집 범위 공개, 자료·통계 출처와 조사 한계를 포함했다.
- `index.html`, `app.html`, `i18n.js`, `style.css`: 홈과 실행 앱 헤더에서 `가이드` 왼쪽에 `아티클`을 추가하고 푸터와 한영 번역에도 연결했다. 링크 증가로 중간 화면 폭에서 중앙 제목과 내비게이션이 겹치지 않도록 721~1040px 헤더를 두 줄 그리드로 전환하고, 모든 헤더 링크와 언어 선택기의 최소 높이를 44px로 맞췄다. 스타일·번역 캐시 버전을 `20260727-1`로 갱신했다.
- `about.html`, `contact.html`, `privacy.html`, `terms.html`, `affiliate-disclosure.html`, `guides/*.html`: 지원 페이지와 모든 독립 가이드의 상단 이동·푸터에서 아티클 허브로 왕복할 수 있도록 내부 링크를 통일했다.
- `amplify.yml`, `sitemap.xml`: Amplify 배포 허용 목록에 `articles/*.html`을 추가하고 사이트맵에 아티클 허브와 첫 글 canonical URL을 등록했다. 루트 수정일은 `2026-07-27`로 갱신했다.
- 검증: 18개 HTML의 내부 링크·로컬 자산·중복 ID·JSON-LD 구문, 아티클 필터 인라인 JavaScript와 `i18n.js` 구문, 사이트맵 15개 URL의 실제 파일·canonical·index 가능 여부, `git diff --check`를 검사했다. 공유 원고와 게시 본문을 단락별로 대조해 통계 출처를 명확히 고친 한 문단 외 누락이 없음을 확인했다.

## 2026-07-26
- `index.html`, `app.html`, `style.css`: AdSense의 `가치가 별로 없는 콘텐츠` 재거절을 기준으로 기존 6개 도구 단일 SPA를 콘텐츠 중심 홈과 실행 전용 앱으로 분리했다. 루트는 클라이밍 측정·호흡 훈련을 주제로 도구 목적, 대상, 한계, 검증 원칙과 최근 변경 기록을 서버 HTML 본문으로 제공하고, 기존 `?tab=` 공유 주소는 `app.html?tab=`으로 이어지게 했다. 실행 앱은 조작 중심 화면이 검색 결과의 대표 콘텐츠가 되지 않도록 `noindex,follow`와 독립 canonical을 적용했다. 실서비스 데스크톱 점검에서 대형 제목의 끝 두 글자가 별도 줄로 떨어지는 조판을 확인해 제목 문구를 간결하게 다듬었다.
- `guides/sequence-generator.html`, `guides/lottery-generator.html`, `guides/index.html`: 비디오 시퀀스 제작과 로또 6/45 조합 생성기의 독립 가이드를 새로 만들고, 실제 처리 순서·입력 예시·알고리즘·실패 조건·브라우저 한계·개인정보 처리·확률 한계를 공개했다. 가이드 허브를 6개 도구와 일치시키고 각 글로 직접 이동하도록 내부 링크와 `CollectionPage` 구조화 데이터를 갱신했다.
- `guides/abrahang.html`, `guides/breathing.html`, `guides/co2-table.html`, `guides/foot-analysis.html`, `about.html`: 기존 가이드에 보이는 작성·기능 검증 주체, 최종 검증일, 실제 입력→결과 예시, 앱 설계값과 외부 근거의 구분, 알려진 측정 한계를 보강하고 `Article` 구조화 데이터를 통일했다. 소개 페이지에는 운영 주체, Who/How/Why, 테스트·문헌 확인 방식, 자동화 사용 범위, 정정 정책과 최근 변경 기록을 사실 범위 안에서 공개했다.
- `i18n.js`, `app.html`: `연마`·`HPMPLab`·`Grind.app`·`Timer.app`으로 나뉘던 브랜드를 HPMPLab의 `연마`로 통일했다. 로또 화면의 `추천 번호`·`구간 디퓨전` 표현은 예측 성능으로 오해하지 않도록 `생성 번호 세트`·`구간 분포 반복 조정`으로 바꾸고, 모든 유효 조합의 당첨 확률이 같다는 안내를 상시 표시한다. 외부 Tistory 핵심 링크와 AliExpress 원격 이미지를 제거해 내부 원본 콘텐츠와 로컬 자산을 우선하게 했다.
- `contact.html`, `privacy.html`, `terms.html`, `affiliate-disclosure.html`, `sitemap.xml`: 문의·정정 요청 범위, Formspree 처리 주체와 삭제 요청, 로컬 파일·측정 데이터 처리, 의료·예측·변환 결과의 한계, 제휴 고지를 구체화했다. 사이트맵은 색인 가능한 콘텐츠 13개와 실제 수정일을 반영하고 실행 앱은 제외했다.
- 검증: 16개 HTML의 내부 링크·로컬 자산·중복 ID·JSON-LD 구문, 사이트맵 13개 URL의 파일·canonical·index 가능 여부, JavaScript 구문, `git diff --check`를 검사했다. 콘텐츠 본문은 홈 535단어, 소개 568단어, 가이드 허브 368단어, 독립 가이드 562~1,041단어로 확인했으며 `Grind.app`·`Timer.app`·외부 Tistory·예측성 추천 문구 잔재가 없는지 검색했다.

## 2026-07-15
- `style.css`, 공통 HTML: 아브라행 링 게이지 상단 좌우의 `현재 하중`·`피크 하중` 라벨과 실시간 측정값 글자를 화면 폭에 따라 더 크게 보이도록 반응형 폰트로 조정하고, 스타일 캐시 버전을 `20260715-3`으로 갱신했다.
- 검증: Chrome Playwright 320·360·600·1024·1440px에서 라벨은 약 12.2~13.4px, 측정값은 약 16.6~19.2px로 확대되는지 확인했다. 긴 추정값 `~110.0kg`도 카드 내부와 문서 폭을 넘지 않았고 중앙 타이머와 겹치지 않았다.
- `style.css`, `index.html`, `about.html`, `privacy.html`, `terms.html`, `contact.html`, `affiliate-disclosure.html`, `guides/*.html`: 모바일에서만 보이던 아브라행 원형 게이지 상단의 `현재 하중`·`피크 하중` 좌우 카드를 데스크톱 브라우저에서도 같은 위치와 크기 규칙으로 항상 표시하도록 반응형 조건을 공통 스타일로 옮기고, 스타일 캐시 버전을 `20260715-2`로 갱신했다.
- 검증: Chrome Playwright에서 360·600·768·1024·1440px 화면의 두 하중 카드가 모두 `display: flex`로 노출되고 원형 게이지 상단 좌우에 대칭 배치되는지 확인했다. 중앙 시간 표시와 충돌, 문서 가로 넘침, 페이지 런타임 오류는 없었다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 무수신 판정 90초보다 여유를 두기 위해 맥스행 시도 사이 휴식을 60~75초(15초 단위), 기본 75초로 줄이고 수행 순서를 `왼손1 → 오른손1 → 왼손2 → 오른손2 → 왼손3 → 오른손3`으로 바꿨다. 저울에는 약 75초마다 다시 하중이 걸리며, 기본값에서 같은 손은 반대손 5초 측정과 두 휴식을 포함해 약 155초를 회복한다. 실행 순서가 바뀌어도 결과 카드와 저장 기록은 기존처럼 왼손 1~3·오른손 1~3 순서로 보이도록 프로토콜 인덱스를 별도로 매핑하고, 과거 120~300초 히스토리의 실제 휴식값은 그대로 보존한다.
- `abrahang-timer.js`, `index.html`, `i18n.js`: 각 시도에서 실제 BLE 수신 시각을 이용한 최고 1초 시간가중 이동평균 힘, 힘 상승 시작 후 200ms 시점 힘, 같은 손의 세 시도 중 최고 1초 힘의 90% 이상을 연속 유지한 최장시간을 계산한다. 손별 `3회 출력 감소율`은 `1 - (세 시도 합 / (3 × 최고 시도))`으로 최고 시도 대비 평균 결손률을 표시한다. 1초/200ms/90% 지표는 샘플 밀도와 최대 공백으로 `유효/추정/샘플 부족`을 구분하고, 추정값에는 `~`를 붙이며 순간 피크를 1초 값으로 대체하지 않는다. 저울은 자세 자체를 판별하지 못한다는 한계를 결과 설명에 명시했다.
- `abrahang-timer.js`: 맥스행 첫 시도만 독립 3초 프리카운트를 사용하고, 두 번째 시도부터는 휴식 마지막 3초를 다음 시도의 기준선 수집과 준비 카운트로 함께 사용한 뒤 곧바로 측정한다. 이로써 휴식 끝의 `3초 + 별도 3초` 중복을 제거했다. 마지막 3초 중 일시정지해도 다음 시도의 기준선 샘플 시각을 정지 시간만큼 보정하며, WH-C06 페이지 복구 payload를 교대 순서와 호환되는 v4로 갱신하고 구형 맥스행 진행 중 복구 데이터는 안전하게 무시한다.
- `abrahang-timer.js`: Abrahangs/Emil의 그립·형태·깊이·한손 수행 손 변경과 맥스행의 손 전환을 실제 프로토콜 단계로 비교한다. 변경이 있으면 기존 3초 카운트의 첫 560Hz 짧은 음만 960Hz·0.72초 주의 알람으로 대체하고, 2초·1초 기본음과 시작음은 유지한다. 로딩 종료 시에는 일반 전환음만 재생해 알람이 일찍 또는 추가로 울리지 않으며, Video의 같은 중간 2손가락 안에서 오픈→하프·깊이 변경도 감지한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 좌우 결과 카드와 히스토리 항목에 날짜·그립을 포함한 접근성 이름을 연결하고, 삭제 버튼도 여러 기록의 대상을 구분할 수 있게 했다. 420px 이하에서는 손별 지표를 세로 목록, 각 시도를 가로 지표 행으로 전환하고, 히스토리는 모바일 2열·넓은 화면 4열로 정렬했다.
- `guides/abrahang.html`, `index.html`, `i18n.js`: 좌우 교대 순서, 75초 기본 휴식과 같은 손 약 155초 회복 구조, 휴식 마지막 3초에 포함되는 단일 프리카운트, 변경 시 첫 음만 길어지는 규칙을 한영 안내와 가이드에 반영했다. 좌우 교대 5초 측정 연구 링크를 추가하고 루트의 i18n/아브라행 스크립트 캐시 버전을 `20260715-2`로 갱신했다.
- 검증: JavaScript 구문과 diff를 검사했다. Chrome Playwright 수동 RAF와 합성 BLE 패킷으로 기본값 75초·전체 6분 48초, `L1/R1/L2/R2/L3/R3` 실행과 다음 손 결과 카드 강조·히스토리 재정렬, 과거 휴식 180초 기록 보존을 확인했다. 다섯 전환 모두 `960Hz 0.72초 → 560Hz 2·1초 → 740Hz 시작음` 한 번씩만 재생되고 별도 프리카운트 없이 휴식에서 바로 측정으로 넘어갔다. 양손의 같은 설정 반복은 기본 짧은 음, 한손 모드의 손 교대와 Paper 그립 변경, Video 중간 2손가락 오픈→하프·깊이 변경은 긴 음으로 구분됐다. 손별 1초 피크 110kg, 200ms 힘 31.5kg, 90% 유지 2.2초, 출력 감소율 10.0%, 유효/추정/샘플 부족, 측정 중 10초 일시정지, 한영 전환, 320/360/1440px 가로 넘침 0과 모드 버튼 44px 이상도 재확인했다.

## 2026-07-14
- `index.html`, `style.css`, `i18n.js`: 기능·디자인·접근성 감사를 반영했다. 앱 탭을 WAI-ARIA 탭 버튼으로 바꾸고 좌우 화살표/Home/End 키 이동, 패널 `hidden` 동기화, 본문 바로가기, 고대비 포커스 링, 폼 접근 가능한 이름, 단계 전환에만 알리는 상태 영역을 추가했다. 모바일 헤더와 탭을 압축하고 320px에서 호흡·발·시퀀스·아브라행 가로 넘침을 제거했으며, 공유 버튼 터치 영역 44px와 `prefers-reduced-motion` 대응을 적용했다. 호흡 화면의 좌측 주파수/중앙 원형 게이지/우측 파란 세로 볼륨 구조는 유지했다.
- `index.html`, `main.js`, `foot-analysis.js`, `sequence-generator.js`, `lottery-generator.js`: 기본 호흡 탭에서 사용하지 않는 CO2·발 핏·시퀀스·로또·아브라행 스크립트를 각 탭 첫 진입 때만 불러오도록 바꿨다. 로또 42만 바이트 이력 JSON도 로또 첫 진입 또는 생성/갱신 때만 한 번 로드하고 버전 URL의 브라우저 캐시를 재사용한다. 지연 로딩 중인 도구 패널은 `aria-busy`와 `inert`로 잠그고 스크립트 실행 직후 탭 활성 이벤트를 다시 보내, 매우 빠른 첫 조작이 초기화 전에 유실되지 않게 했다. 시퀀스의 런타임 오류·파일 정보·프리셋 상태 문구를 한글/영문으로 통일하고, 모션 감소 설정에서는 강제 부드러운 스크롤과 호흡 버블 변형을 멈춘다.
- `index.html`, `foot-analysis.js`, `i18n.js`, `guides/foot-analysis.html`, `guides/index.html`, `about.html`, `privacy.html`: 기존 발 사진 기능이 실제 픽셀 측정을 하지 않는데도 사진 기반 치수·형태 분석처럼 보이던 문제를 제거했다. 사진은 선택적 비교 기록으로 분리하고, 좌우 실측 길이와 발볼·발등·아치·발가락 자가 체크를 그대로 표시하는 암벽화 핏 체크리스트로 교체했다. 가짜 seed/너비 mm/브랜드 모델 점수/사이즈 환산표와 과거 가짜 계산 이력의 노출을 삭제하고, 손상된 로컬 이력을 안전하게 처리하며 실제 착화와 제조사 공식표를 우선하도록 한계를 명시했다.
- `index.html`, `i18n.js`, `guides/breathing.html`, `guides/co2-table.html`: 루트 페이지의 과도한 효과 표현과 숨참기 처방처럼 보이는 문구를 정리했다. 호흡은 이완 보조 도구이며 의료 치료를 대신하지 않는다는 NCCIH 기준, CO2 테이블은 과호흡 금지·수중 단독 훈련 금지·버디 필요라는 DAN 안전 기준을 독립 가이드에 출처와 함께 추가했다. 루트의 대용량 인라인 가이드 복제를 제거하고 독립 가이드 링크로 바꿔 초기 DOM을 줄였다.
- `amplify.yml`, `customHttp.yml`, `404.html`: Amplify 산출물을 실제 서비스 파일과 헤더 설정 40개로 제한해 `ProjectInfo.MD`, `Blueprint.MD`, `Agent.MD`, `docs/**`, 훅/스크립트, 미사용 MP3와 원본 이미지를 공개 배포에서 제외했다. HSTS, SAMEORIGIN, nosniff, Referrer-Policy, 카메라만 허용하는 Permissions-Policy를 추가하고 외부 자산이 없는 반응형 404 페이지를 만들었다. Amplify 앱 규칙은 apex를 `www`로 보내는 301과 `/<*> -> /404.html`의 `404-200` rewrite로 정리해, 없는 파일과 내부 문서 요청이 원래 URL을 유지하면서 실제 HTTP 404와 새 오류 페이지를 반환하도록 적용했다.
- `.githooks/pre-commit`, `scripts/check_worklog_guard.sh`: 코드·설정 변경을 커밋할 때 `docs/현재작업중.md`가 함께 스테이징되지 않으면 커밋을 막는 저장소 로컬 가드를 추가하고 `core.hooksPath=.githooks`를 설정했다.
- 검증: 전체 JavaScript 구문과 diff, 13개 HTML의 로컬 링크/ID/ARIA 연결, Amplify 허용 목록을 검사했다. Chrome Playwright에서 320/360/1440 및 미디어쿼리 경계 21개 폭의 6개 탭 가로 넘침 0, 키보드 탭 전환, 한영 전환, 사진 없는 핏 체크리스트, 로또 5세트 생성, 시퀀스/아브라행 지연 로딩, 모션 감소 설정, 런타임 오류 0을 확인했다. Amplify main 배포 작업 317이 성공했고 실서비스에서 apex 301, 보안 헤더, 내부 문서/없는 파일의 HTTP 404 및 전용 404 본문을 재확인했다.

## 2026-07-08
- `index.html`, `abrahang-timer.js`: 아브라행 타이머에서 다음 로딩 세트의 그립이 직전 세트와 달라질 때 일반 전환음 대신 짧은 3음 패턴을 재생하도록 바꿨다. 같은 그립 반복 세트는 기존 전환음을 유지하고, 완료 알람의 긴 3회 비프는 그대로 유지한다.
- `main.js`, `index.html`: 호흡 타이머의 마지막 설정을 `localStorage`에 저장/복원하도록 추가했다. 들숨/날숨 시간, 동기화 여부, 자동 증가 on/off와 증가량, 차크라 선택, 볼륨을 저장하며 새로고침 후에도 마지막 설정값으로 시작한다. 진행 중 라운드/경과 시간은 저장하지 않고 설정만 유지한다.
- `index.html`, `style.css`, `i18n.js`: 모바일 첫 화면에서 기존 상단 네비게이션/앱 탭과 중복되어 보이던 `훈련 도구와 사용 가이드` 안내 박스를 제거했다. 상단 네비게이션의 `앱 가이드` 링크도 빼고 `가이드` 하나만 남겨 중복감을 줄였다. 루트 소개 문구와 별도 가이드 페이지는 유지하고, 가이드 접근은 상단 `가이드` 메뉴와 푸터/사이트맵으로 남겼다.
- `video-to-sprite-sheet.html`, `guides/*.html`: AdSense 심사 리스크를 줄이기 위해 사이트 구조에 편입되지 않은 `video-to-sprite-sheet.html`에 `noindex,nofollow`를 추가했다. 가이드 허브와 아브라행/호흡/CO2 테이블/발 분석 가이드는 루트 소개 문구를 늘리지 않고, 각 앱의 실제 사용 순서, 설정 예시, 기록 해석, 안전 기준, 한계 설명을 독립 페이지 본문에 보강했다. `https://hpmplab.com/`는 `https://www.hpmplab.com/`로 리다이렉트되는 것을 확인했으며 현재 HTTPS apex 응답은 302다.
- `index.html`, `style.css`, `i18n.js`: AdSense 콘텐츠 신호용 사이트 개요 블록이 첫 화면을 과도하게 차지하지 않도록 축소했다. 제목/설명 문구를 짧게 줄이고, 가이드 링크 4개는 큰 버튼 대신 작은 칩 링크로 표시하며 모바일에서는 세로로 자연스럽게 접히도록 조정했다.

## 2026-07-07
- `ads.txt`: AdSense 게시자 ID `pub-5536720144858364`를 Google 공식 ads.txt 형식으로 루트에 추가했다.
- `index.html`, `style.css`, `i18n.js`, `about.html`, `privacy.html`, `terms.html`, `contact.html`, `affiliate-disclosure.html`, `guides/*.html`, `robots.txt`, `sitemap.xml`: AdSense의 `가치가 별로 없는 콘텐츠` 거절 사유를 기준으로 루트 페이지에 사이트 설명/가이드 허브 링크/canonical/구조화 데이터를 추가하고, 크롤링 가능한 독립 가이드 5개와 소개/문의/개인정보처리방침/이용약관/제휴고지 페이지를 추가했다. 표준이 아닌 복사성 `adsense.txt`는 삭제하고 `robots.txt`, `sitemap.xml`을 배포 대상에 추가했다.
- `index.html`, `style.css`, `i18n.js`: 모바일 App Guide의 더 읽기 본문 표가 카드 폭 밖으로 밀리는 문제를 수정했다. 번역 적용 후 표 헤더를 각 셀의 모바일 라벨로 주입하고, 작은 화면에서는 표를 행 단위 카드 레이아웃으로 전환해 Abrahang 가이드 표가 우측으로 넘치지 않도록 했다.
- `index.html`, `abrahang-timer.js`: Abrahang 훈련 히스토리 목록의 하중 칸을 기존 목표하중 단독 표시에서 `목표하중 / 수행평균부하` 형식으로 바꿨다. 수행평균부하는 새 기록의 세트별 샘플 합계/샘플 수를 우선 사용하고, 샘플 합계가 없는 기존 기록은 그립/손별 샘플 또는 세트별 최대하중 평균으로 폴백한다.
- `index.html`, `main.js`: 탭 공유 버튼이 `?tab=abrahang#abrahang`처럼 hash를 포함한 주소를 만들지 않고 `?tab=abrahang` 형태의 깨끗한 공유 URL만 복사하도록 수정했다. Instagram/Facebook이 기존 링크의 `#abrahang`를 `%23abrahang`로 query 안에 붙여도 `tab` 값에서 `#` 뒤를 잘라 정상 Abrahang 탭으로 들어가도록 URL 파서를 보강했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: Abrahang의 별도 `오늘의 순서` 영역을 제거하고, 타이머 하단 현재 동작 설명 박스 안에 세트 묶음만 짧은 칩 형태로 표시하도록 바꿨다. 기존 상세 설명 리스트는 없애고, 현재/대기/휴식/완료 상태에 맞춰 해당 묶음만 강조한다.
- `index.html`, `style.css`: 모바일 Abrahang 화면에서 강도 설정 영역이 훈련 히스토리보다 먼저 나오도록 히스토리를 타이머 패널 내부에서 독립 그리드 영역으로 옮겼다. 데스크톱에서는 히스토리가 계속 왼쪽 하단에 남고, 모바일에서는 `타이머 -> 강도 설정 -> 훈련 히스토리` 순서로 표시된다.
- `index.html`, `style.css`: 모바일 Abrahang 화면에서 `로딩/휴식/반복/다음 세션` 요약 카드가 1열로 쌓여 첫 화면을 과도하게 차지하던 문제를 수정했다. 모바일에서도 4개 요약을 가로 4열로 유지하고 패딩/글자 크기를 줄여 세로 높이를 낮췄다. 안전 기준은 데스크톱에서는 기존 설정 패널에 유지하고, 모바일에서는 오늘의 순서 아래 App Guide 바로 위에 별도 표시되도록 배치했다.
- `index.html`, `style.css`, `abrahang-timer.js`: 모바일 화면에서 타이머를 보며 실시간 하중도 확인할 수 있도록 링 타이머 상단 좌우에 현재 하중/피크 하중 배지를 추가했다. 배지는 모바일 폭에서만 표시되며, 기존 하단 좌우 그립 이미지와 상하 대칭이 되도록 같은 다이얼 스테이지 안에 배치했다. 현재 하중은 크레인 저울에서는 저울값 자체, 발판 저울에서는 체중-저울값 기준 실제 하중을 표시한다.
- `abrahang-timer.js`, `style.css`, `i18n.js`, `index.html`: 훈련 히스토리 상세 드롭다운에 `그립별 평균 하중` 영역을 추가했다. 각 그립 카드에는 좌/우 손 아이콘을 배치하고, 새로 기록되는 세션은 수신 샘플 합계/샘플 수 기준 평균 하중을 표시한다. 기존 기록처럼 샘플 합계가 없는 경우에는 저장된 세트별 최대하중의 평균으로 폴백해 현재 기록된 데이터에서 가능한 평균을 보여준다.
- `abrahang-timer.js`, `i18n.js`: 아브라행 훈련 히스토리의 측정 결과를 손별로 분리해 저장하도록 확장했다. 한손 모드에서는 현재 수행 손의 그립별 최대하중/달성률만 해당 손에 누적하고, 양손 모드에서는 같은 측정치를 왼손/오른손 양쪽에 동일하게 기록한다. 결과분석 그래프는 기존 그립별 4개 라인에서 좌/우 손을 포함한 8개 라인으로 확장했으며, 히스토리 상세에는 왼손/오른손 최대 하중을 함께 표시한다. WH-C06 페이지 새로고침 복구 payload에도 손별 측정 누적값을 포함한다.
- `index.html`, `style.css`, `abrahang-timer.js`, `assets/images/abrahang-grip-candidates/20260706/*-display.webp`: 기존 그립 PNG 원본은 백업용으로 유지하고, 실제 앱 표시에는 320px 정사각형 WebP 표시본을 사용하도록 바꿨다. 표시본은 현재 UI에서 잘려 보이는 손+행보드 중심 영역만 크롭해 생성했으며, 각 파일은 약 6.8~7.8KB 수준이다. CSS의 기존 132% 확대/이동 크롭은 제거하고 WebP 표시본을 그대로 채우게 했다.

## 2026-07-06
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06의 같은 페이지 내 `다시 연결`/`재시작` 동선이 실제 Chrome Web Bluetooth 광고 감시 복구에 실패한다는 관측을 기준으로, WH-C06 세션이 이미 있거나 수신이 멈춘 상태에서는 버튼을 `페이지 새로고침`으로 표시하도록 단순화했다. 최초 연결 전에는 기존처럼 `WH-C06 / IF_B7 연결(beta)`로 장치 목록을 열고, 이후 끊김/첫 패킷 없음/복귀 후 수신 없음 상태에서는 타이머 상태를 `sessionStorage`에 저장한 뒤 페이지 새로고침으로 Web Bluetooth 실행 컨텍스트를 새로 만드는 안내와 동작을 사용한다. 별도 `WH-C06 완전 재접속` 버튼은 제거했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 블루투스 저울 패널 안의 크레인 저울 구매 링크/Chrome flags 안내와 오른쪽 패널의 `다음 권장 시작` 카드 위치를 서로 교환했다. WH-C06의 같은 페이지 내 `다시 연결`/기존 감시 재시작이 실제 기기에서 계속 실패한다는 관측에 맞춰, 끊김 상태에서는 `WH-C06 완전 재접속` 버튼을 추가로 표시한다. 이 버튼은 현재 타이머 세션 상태를 `sessionStorage`에 임시 저장한 뒤 Abrahang 탭을 새로 열어 Web Bluetooth 실행 컨텍스트 자체를 초기화하고, 새로고침 후 타이머 상태를 복원한 다음 WH-C06 연결을 다시 누르게 한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 일반 방문자가 WH-C06 수신 기능을 준비할 수 있도록 Chrome flags 안내 블록을 블루투스 저울 영역에 추가했다. `chrome://flags/#enable-experimental-web-platform-features` 열기 링크와 주소 복사 버튼을 함께 제공하며, Chrome이 내부 설정 주소 이동을 막는 경우 새 탭 주소창에 붙여넣도록 설명한다. Web Bluetooth 부분 reload 우회는 단순 DOM 교체로는 불가능하고, 저울 연결부를 같은 origin iframe으로 분리한 뒤 iframe 자체를 갈아끼우는 방식만 의미가 있다는 판단을 남겼다. 이 방식은 iframe 안 버튼 클릭으로 `requestDevice()` 사용자 활성화를 만족시키고 parent와 `postMessage`로 하중값을 주고받는 별도 구조 변경이 필요하다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 창 복귀 후 `WH-C06 다시 연결`에서 장치 목록은 열리지만 선택 후 광고 패킷이 들어오지 않는 문제를 다시 좁혔다. 기존 구현은 장치 선택창을 열기 전 이전 `watchAdvertisements()`를 완전히 정리하지 않았고, 첫 패킷 타임아웃 후 다시 `재시작` 루프로 돌아갔다. 재연결 클릭 시 이전 WH-C06 감시 해제를 먼저 시작하고 장치 선택 후에도 한 번 더 `unwatchAdvertisements()`를 강제한 뒤 새 감시를 붙이도록 바꿨으며, 첫 패킷 타임아웃 후에도 `WH-C06 다시 연결`로 장치 목록을 다시 여는 상태를 유지하게 했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 기존 장치 감시 재시작이 실제 기기에서 복구되지 않는 상황에 맞춰, 창 복귀/수신 끊김 감지 시 자동 재시작 대신 `다시 연결` 상태로 전환하도록 바꿨다. 이 상태에서 버튼을 누르면 `requestLEScan()`이나 기존 장치 재시작이 아니라 최초 연결과 같은 `requestDevice()` 장치 목록을 다시 열어 IF_B7을 재선택하게 한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 창 전환 직후 WH-C06 수신 끊김을 앱이 감지하는 점을 복구 트리거로 사용하도록 바꿨다. 기존 WH-C06 장치 객체가 있으면 버튼 클릭이나 Chrome 복귀/수신 끊김 감지 시 새 권한창이나 장치 목록을 열지 않고 기존 장치의 광고 감시를 `unwatchAdvertisements()` 후 `watchAdvertisements()`로 재시작한다. 새 BLE 광고 스캔은 기존 장치 객체가 없는 leScan 세션에만 남겨둔다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 최초 연결에서 `requestLEScan()` 권한창이 떠 장치 목록이 보이지 않는 문제를 수정했다. WH-C06 세션이 아직 없을 때는 기존처럼 Chrome 장치 목록(`requestDevice`)에서 IF_B7을 선택하게 하고, 이미 WH-C06 세션이 있는 상태에서 끊긴 뒤 누르는 새로 스캔에만 BLE 광고 스캔 복구 경로를 사용한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 새로 스캔이 여전히 페어링된 장치 캐시/기존 장치 감시 상태에 묶일 수 있어, 지원 브라우저에서는 `requestLEScan({ keepRepeatedDevices: true })` 기반의 새 BLE 광고 스캔을 우선 사용하도록 바꿨다. 새 BLE 스캔은 모든 광고를 받아 앱 내부에서 WH-C06 manufacturer data만 필터링하며, 해당 API를 사용할 수 없는 경우에만 기존 `requestDevice()`/`watchAdvertisements()` 경로로 폴백한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 새로 스캔을 페이지 새로고침 직후 첫 연결과 같은 흐름에 가깝게 바꿨다. 버튼 클릭 시 먼저 Chrome 장치 선택창을 열어 사용자 제스처를 보존하고, 선택 후 기존 광고 감시를 충분히 정리한 뒤 새 `watchAdvertisements()` 세션을 시작한다. Chrome 복귀/페이지 복원 시에는 기존 감시를 끊지 않고 수신 재확인만 수행하며, 관련 안내 문구도 실제 동작 기준으로 수정했다.
- `index.html`, `style.css`, `i18n.js`: 아브라행 블루투스 저울 패널에 크레인 저울 제휴 링크를 작은 장비 링크 형태로 추가했다. 트레이닝 조작을 방해하지 않도록 측정값 아래 설명문 위에 배치하고, `제휴 링크` 표기를 함께 표시한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 훈련 히스토리 상세 드롭다운에 `결과분석` 체크박스를 추가하고, 체크한 세션만 시간순으로 모아 4손가락/앞 3손가락/앞 2손가락/중간 2손가락의 최대하중과 하중달성률을 캔버스 그래프로 표시하도록 했다.
- `abrahang-timer.js`: 세션 완료 기록에 전체 최대하중뿐 아니라 1~20세트별 측정 최대하중과 그립별 요약 측정치를 함께 저장하도록 확장했다. 기존 히스토리에 그립별 데이터가 없으면 분석 그래프에서는 측정 데이터 없음 상태로 처리한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 링 타이머 좌우 바깥쪽 하단에 손가락 그립 개별 PNG를 배치했다. 오른쪽은 원본 오른손 이미지를 사용하고, 왼쪽은 CSS로 반전해 표시한다.
- `abrahang-timer.js`: 현재/다음 세트의 그립 종류에 맞춰 4손가락, 앞 3손가락, 앞 2손가락, 중간 2손가락 이미지를 전환한다. 한손 모드에서는 선택한 손을 기준으로 라운드마다 좌우를 교대하며, 프리카운트/휴식 중에는 다음 수행 손을 미리보기 점멸로 표시하고 실제 로딩 중에는 수행 손을 활성 점멸로 표시한다.
- `assets/images/abrahang-grip-candidates/20260706/*.png`: 앱에서 사용할 개별 손가락 그립 PNG 편집본을 반영했다. `Hangboard_fingers.psd` 작업 원본은 커밋 대상에서 제외한다.

## 2026-07-05
- `index.html`, `style.css`, `abrahang-timer.js`: 아브라행 `오늘의 순서`에서 인접한 동일 세트를 `1~6` 같은 범위 묶음으로 렌더링하도록 바꿨다. 현재 진행/휴식 미리보기/완료 상태는 해당 세트 범위가 포함된 묶음 카드에 적용되며, CSS/스크립트 캐시 버전을 갱신했다.

## 2026-04-02
- `index.html`: 브라우저 탭 아이콘이 표시되도록 favicon 및 apple touch icon 링크를 추가했다.
- `assets/icons/favicon-16x16.png`: 원본 보석 이미지를 16x16 파비콘으로 리사이즈해 추가했다.
- `assets/icons/favicon-32x32.png`: 원본 보석 이미지를 32x32 파비콘으로 리사이즈해 추가했다.
- `assets/icons/favicon-48x48.png`: 원본 보석 이미지를 48x48 파비콘으로 리사이즈해 추가했다.
- `assets/icons/favicon.ico`: 브라우저 호환용 멀티사이즈 ico 파일을 추가했다.
- `assets/icons/apple-touch-icon.png`: 모바일 홈 화면용 180x180 아이콘을 추가했다.

## 2026-04-03
- `index.html`: 헤더 왼쪽에 파비콘 이미지를 홈 버튼처럼 배치하고, 스타일/스크립트 캐시 버전을 갱신했다.
- `style.css`: 헤더 홈 아이콘 버튼 스타일을 추가해 기존 중앙 타이틀 레이아웃을 유지하면서 홈 이동 동선을 만들었다.
- `main.js`: 호흡트레이너 사운드 재생이 크롬에서 실패해도 Web Audio 톤으로 폴백되도록 오디오 초기화와 재생 로직을 보강했다.

## 2026-06-19
- `index.html`: CO2 테이블 스톱워치의 `Auto`/`Lap` 버튼을 제거하고 숨참기 홀드시간, 휴식시간 설정 UI를 추가했다.
- `stopwatch.js`: `Start` 버튼이 설정값 기반 자동 CO2 루틴을 시작하도록 바꾸고, 휴식 시간이 라운드마다 15초씩 감소하되 15초 아래로 내려가지 않도록 했다.
- `style.css`: CO2 테이블 설정 입력과 5초 증감 버튼 레이아웃을 추가했다.
- `i18n.js`: CO2 테이블 자동 진행, 숨참기/휴식 설정, 차트/기록 문구를 한글/영문으로 갱신했다.
- `stopwatch.js`, `style.css`: CO2 차트 링 확장 효과가 찌그러지거나 캔버스 밖에서 잘리지 않도록 차트 내부 패딩과 캔버스 표시 비율을 조정했다.
- `main.js`, `stopwatch.js`: Android Chrome 등 Screen Wake Lock 지원 브라우저에서 호흡/CO2 타이머 작동 중 화면이 꺼지지 않도록 wake lock 요청/해제 흐름을 추가했다.
- `stopwatch.js`, `style.css`: CO2 기록 영역의 활성 칸 점멸 효과를 남은 시간에 따라 오른쪽부터 줄어드는 게이지 배경으로 변경했다.
- `style.css`: CO2 기록 영역의 게이지 배경을 유지하면서 활성 칸 점멸 효과가 함께 보이도록 펄스 애니메이션을 복원했다.
- `index.html`, `style.css`: CO2 테이블 시간 설정의 상단 라벨 줄을 숨기고 중앙 입력칸 배경에 `hold`/`rest` 표기를 넣어 설정 영역 높이를 줄였다.
- `main.js`, `stopwatch.js`, `index.html`: CO2 테이블 자동 루틴이 끝나거나 정지/리셋될 때 `running` 상태 전환과 함께 Screen Wake Lock이 확실히 해제되도록 정리하고 스크립트 캐시 버전을 갱신했다.
- `index.html`, `style.css`: CO2 시간 설정 중앙 입력 영역에서 `hold`/`rest`와 시간값을 좌우 배치로 바꾸고, 차트/게이지 색상과 맞춰 hold는 붉은 계열, rest는 푸른 계열 배경으로 조정했다.
- `index.html`, `style.css`: CO2 시간 설정 중앙 입력 영역에서 `hold`/`rest` 옆 시간값을 각 값 칸 안에서 가운데 정렬되도록 조정하고 스타일 캐시 버전을 갱신했다.
- `stopwatch.js`, `i18n.js`, `index.html`: CO2 자동 루틴이 휴식 15초에 도달해도 자동 종료되지 않고, 이후 라운드는 15초 휴식으로 계속 진행되도록 수정하고 안내 문구와 스크립트 캐시 버전을 갱신했다.
- `main.js`, `index.html`: 호흡타이머 사운드를 파일 재생 대신 Web Audio 합성 톤 기본값으로 바꾸고, 소리 크기 슬라이더가 합성 톤의 master gain에 직접 적용되도록 정리했다.
- `index.html`: URL에 유효한 탭 값이 없을 때 빈 selector로 탭 검증을 시도하지 않도록 방어 로직을 보강했다.

## 2026-06-24
- `index.html`, `style.css`, `i18n.js`: 호흡 타이머 컨트롤에 `자동 +0.1초` 토글 버튼을 추가하고 한국어/영어 문구와 활성 상태 스타일을 연결했다.
- `main.js`: 날숨이 끝나고 다음 들숨 라운드로 넘어갈 때 라운드 번호를 증가시키고, 자동 토글이 켜져 있으면 들숨/날숨 시간을 라운드마다 0.1초씩 증가하도록 했다.
- `index.html`: 자동 0.1초 증가값이 유효한 숫자 입력으로 유지되도록 들숨/날숨 입력 step을 0.1초로 조정하고 스크립트 캐시 버전을 갱신했다.
- `index.html`, `style.css`, `main.js`, `i18n.js`: 자동 버튼 좌우에 증가량 조절용 꺾쇠 버튼을 추가하고, 자동 증가량을 0.1초 단위로 조절하되 0.1초 이하로 내려가지 않도록 왼쪽 버튼 비활성 상태를 적용했다.

## 2026-06-25
- `index.html`, `style.css`, `main.js`, `i18n.js`: 호흡 트레이너 원형 게이지 중앙에 현재 단계의 목표 설정시간을 진행시간보다 작은 글씨로 표시하고, 입력값/단계/언어 변경 시 즉시 갱신되도록 했다.
- `main.js`, `index.html`: 호흡 사운드 전환/정지 시 기존 oscillator를 즉시 끊지 않고 짧게 릴리즈 페이드아웃하도록 바꾸고, 새 톤도 부드러운 어택으로 시작하게 조정해 팝 노이즈 원인을 제거했다. 날숨 톤은 거친 triangle 파형 대신 sine 파형의 하강 톤으로 바꾸고, 볼륨 슬라이더 변경도 짧게 스무딩하도록 했다.

## 2026-06-26
- `index.html`, `style.css`: 호흡 트레이너 설정 영역을 들숨/날숨 각각 한 줄 구성으로 압축하고, 원형 게이지 안 목표시간 글자 크기를 키웠다.
- `index.html`, `style.css`, `main.js`, `i18n.js`: 호흡 트레이너의 `소리 크기` 라벨을 `주파수`로 바꾸되 볼륨 슬라이더는 유지하고, 차크라 선택 이미지를 추가해 선택한 차크라의 솔페지오 Hz를 날숨 톤으로, 2배 배음을 들숨 톤으로 적용하도록 했다.
- `main.js`, `index.html`: 차크라 주파수 적용 후 사라진 호흡 사운드 시작부 피치 업/다운 효과를 복원하고, 캐시 버전을 갱신했다.
- `main.js`, `index.html`, `i18n.js`: 들숨 사운드의 2배 배음 적용을 제거하고, 날숨과 동일하게 선택한 차크라의 원래 Hz로 재생/표시되도록 수정했다.
- `index.html`, `i18n.js`: 차크라 선택 영역에서 들숨/날숨 주파수를 따로 표기하지 않고 `차크라명 · Hz` 형식으로만 표시되도록 정리했다.
- `index.html`, `i18n.js`: 호흡 타이머 앱 가이드에 차크라 주파수 포스트를 추가하고, 더보기 본문에 차크라별 위치/의미/색/만트라/솔페지오 Hz와 사용 주의점을 정리했다.

## 2026-07-02
- `index.html`, `style.css`, `i18n.js`: 호흡 트레이너의 차크라 주파수 선택기를 세로형으로 바꾸고 원형 게이지 왼쪽에 배치했다. 볼륨 슬라이더는 설정 패널 가운데 정렬하고 레이블을 `볼륨`으로 복구했다.
- `index.html`: 세로형 차크라 선택기의 시각 순서를 인체 위치 기준에 맞춰 사하스라라/Crown이 위, 물라다라/Root가 아래로 보이도록 정렬했다.
- `index.html`, `style.css`: 호흡 트레이너 상단을 `주파수 선택기 / 원형 게이지 / 세로 볼륨 슬라이더` 3열 구조로 정리해 원형 게이지가 가운데 축에 오도록 조정했다.
- `index.html`, `style.css`: 세로 볼륨 슬라이더가 사이트 공통 초록 accent를 상속받지 않도록 볼륨 슬라이더 색상을 기존 파란색 계열로 복구하고 스타일 캐시 버전을 갱신했다.
- `index.html`: 새 `abrahang` 탭과 앱 가이드 글을 추가하고, 아브라행 행보드 타이머 스크립트와 캐시 버전을 연결했다.
- `abrahang-timer.js`: 논문 기반 10초 로딩/20초 휴식 20회 프로토콜과 영상 기반 10초 로딩/50초 휴식 10회 프로토콜을 전환할 수 있는 아브라행 타이머를 추가했다. 전환음, 루틴 복사, 강도 목표, 완료 후 6시간 회복 기록을 포함했다.
- `style.css`: 아브라행 타이머 전용 원형 진행 게이지, 프로토콜 전환 버튼, 안전 기준, 순서표, 모바일 레이아웃 스타일을 추가했다.
- `i18n.js`: 아브라행 타이머 탭/버튼/상태/가이드 문구를 한국어와 영어 번역 키로 추가하고, 관련 논문과 요청 영상을 가이드 출처로 연결했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 타이머 설정에 체중 입력을 추가하고, 강도 목표 퍼센트를 체중 기준 kg 목표 하중으로 즉시 환산해 표시하도록 했다. 입력한 체중은 브라우저 로컬 저장소에 보존된다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 강도 목표 범위를 체중 30~80%로 확장하고, 논문 모드는 50%, Emil 모드는 70%를 기본값으로 자동 적용하도록 했다.
- `abrahang-timer.js`, `style.css`, `index.html`: 아브라행 휴식 중 상태 카드가 다음 세트 미리보기를 표시하도록 바꾸고, 오늘의 순서 하이라이트가 진행 단계에 맞춰 이동하도록 수정했다. 실제 로딩 중인 세트에만 점멸 효과를 적용하고 휴식 중 프리뷰는 비점멸 하이라이트로 구분했다.
- `index.html`, `i18n.js`: 아브라행 논문 모드 표시명을 `논문 10분`/`Paper 10 min`에서 `Abrahangs`로 통일하고 i18n 캐시 버전을 갱신했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 `루틴 복사` 버튼과 클립보드 복사 로직을 제거하고, 시작/안전 기준 문구를 하중 통제와 가벼운 워밍업 기준에 맞춰 수정했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 설정에 `저울 연결(beta)` 패널을 추가하고, 표준 BLE Weight Scale Service 연결/알림 수신/실시간 저울값 표시와 체중 대비 실제 손가락 하중 계산을 연결했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 사용 중인 Weiheng WH-C06/IF_B7 행잉 스케일이 표준 Weight Scale Service가 아니라 광고 패킷으로 무게를 보내는 구조라서, WH-C06 전용 연결 버튼과 `watchAdvertisements()` 기반 manufacturer data 파싱 경로를 추가했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: WH-C06 연결 후 긴 오류 안내가 버튼 영역에 눌려 한 글자씩 줄바꿈되는 문제를 막기 위해 저울 패널 헤더를 한 열 구조로 고정하고, 상태 문구는 짧게, Chrome flag 안내는 설명 문구로 분리했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: WH-C06가 목록에는 뜨지만 실시간 저울값이 나오지 않는 경우를 구분하기 위해 브라우저의 `watchAdvertisements()` 지원 여부를 기기 선택 전에 진단하고, 미지원 브라우저에서는 Chrome 실험 기능 활성화/완전 재시작 안내를 표시하도록 했다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 강도 설정에 한손 모드를 추가해 목표 하중을 절반으로 계산하도록 하고, 저울 방식을 발판 저울/크레인 저울로 분리했다. 크레인 저울 모드에서는 저울값을 현재 하중으로 표시하고 목표 저울값 대신 목표 하중을 표시하며, WH-C06 광고 패킷 대기 시간은 10초에서 1분으로 늘렸다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 광고 이벤트를 받은 즉시 수신 타이머를 갱신하도록 바꾸고, 무게값이 없는 광고 패킷/수신 공백/1분 이상 미수신 상태를 분리해 표시했다. 수신 공백이 길어지거나 탭이 다시 표시되면 연결을 끊지 않고 `watchAdvertisements()` 감시를 재시작하도록 보강했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 타이머 최초 시작 시 3초 프리카운트와 카운트다운/시작 신호음을 거친 뒤 첫 로딩 세트가 시작되도록 했다. WH-C06는 수신 공백이 생겨도 자동 `unwatchAdvertisements()`를 호출하지 않고 감시를 유지하며, 이미 선택된 WH-C06에서 버튼을 누르면 장치 선택창 대신 현재 장치의 광고 감시만 다시 확인하도록 바꿨다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 강도 안내 문구의 `논문 모드` 표기를 `Abrahangs 모드`로 수정했다. WH-C06 광고 수신이 길게 멈추면 더 이상 연결 중으로 표시하지 않고 수신 중단/새로 스캔 필요 상태로 전환하며, WH-C06 버튼은 기존 장치 객체 재사용 대신 새 장치 선택 스캔을 열도록 변경했다.
- `index.html`, `abrahang-timer.js`: 실제 아브라행 수행 중 20초 휴식 구간에서 WH-C06가 광고 패킷을 보내지 않을 수 있어 수신 중단 판정 시간을 90초로 늘렸다. 프리카운트 종료 및 각 휴식 종료 후 로딩에 들어갈 때 `watchAdvertisements()`를 다시 확인해 브라우저 광고 감시가 조용히 멈춘 경우를 복구하도록 했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 실제 수행 중 행 로딩 타이밍에 WH-C06 값 수신이 멈추는 경우를 보강했다. 로딩 중 4초 이상 광고 패킷이 없으면 `navigator.bluetooth.getDevices()`로 Chrome에 이미 허가/페어링된 IF_B7 장치를 찾아 새 `BluetoothDevice` 객체로 `watchAdvertisements()`를 다시 시작하고, 해당 상태를 저울 패널에 표시하도록 했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 새로 스캔 버튼이 페어링된 장치 캐시를 먼저 재사용해 실제 스캔이 열리지 않는 문제를 수정했다. 수동 WH-C06 버튼은 항상 Chrome 장치 선택창을 열고, 자동 복구에서만 페어링된 IF_B7 재감시를 시도한다. 광고 감시 재시작 시 `unwatchAdvertisements()` 완료를 기다린 뒤 `watchAdvertisements()`를 호출해 늦은 unwatch가 새 watch를 끊는 상황을 줄였고, 자동 복구 후 12초 동안 첫 패킷이 없으면 새 스캔 필요 상태로 전환하도록 했다.
- `index.html`, `abrahang-timer.js`: 아브라행 카운트다운/전환 신호음의 기존 Web Audio 게인이 카운트다운 `0.12`, 전환/완료 `0.18`로 최대값이 아니어서, 신호음 게인을 `1.0`으로 상수화해 최대 볼륨으로 재생되도록 조정하고 스크립트 캐시 버전을 갱신했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: Chrome에서 다른 프로그램으로 전환했다가 돌아온 뒤 WH-C06 광고 수신이 멈추고 새 스캔으로도 값이 복구되지 않는 상황을 보강했다. `visibilitychange`와 `window blur/focus`를 함께 감지해 Chrome 복귀 후에는 기존 광고 감시를 `unwatch`로 끊지 않고 감시만 확인하며, 쉬는 구간에서는 실패 판정을 미루고 다음 로딩 단계에서 새 패킷 도착을 15초 동안 확인한다. 수동 WH-C06 새 스캔은 기존 WH-C06 객체의 `unwatchAdvertisements()`를 기다리지 않고 로컬 상태를 분리한 뒤 Chrome 장치 선택창을 열도록 바꿨다.

## 2026-07-03
- `index.html`, `style.css`: 로또번호 생성기 액션 영역에 눈에 덜 띄는 `수동입력` 버튼과 45개 번호 선택 패널을 추가하고, 6개 선택 완료 시에만 `심상화` 비밀 버튼이 나타나도록 구성했다. 호흡 타이머 원형 게이지 아래에는 선택한 번호 6개를 표시하는 숨김 영역을 추가했다.
- `lottery-generator.js`: 수동 선택 상태, 6개 번호 검증, 브라우저 로컬 저장소 저장, `lottery:vision` 이벤트 발행, 호흡 타이머 탭 전환 흐름을 추가했다.
- `main.js`: 로또 수동 번호 저장값과 `lottery:vision` 이벤트를 받아 호흡 타이머 원형 게이지 아래에 번호볼을 렌더링하도록 연결했다.
- `i18n.js`: 로또 수동입력/심상화 버튼과 호흡 타이머 심상화 번호 라벨 번역 키를 추가했다.
- `docs/현재작업중.md`: 이번 코드 변경 파일과 작업 내용을 기록했다.
- `index.html`, `style.css`, `i18n.js`, `lottery-generator.js`: 로또 수동입력의 `지우기` 버튼을 `저장하기`로 바꾸고, 저장된 6개 번호 묶음을 리스트로 관리하도록 수정했다. 저장 리스트 행을 선택하면 희미한 `심상화` 버튼이 나타나며, 행별 삭제 버튼으로 리스트 항목만 삭제할 수 있다.
- `main.js`, `lottery-generator.js`: 심상화 번호는 더 이상 브라우저 저장소에서 호흡 타이머로 복원하지 않고, 저장 리스트의 `심상화` 버튼을 누른 순간에만 `lottery:vision` 이벤트로 전달하도록 변경했다. 새로고침 또는 다른 앱 탭 이동 시 호흡 타이머의 심상화 표시는 사라진다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: WH-C06 연결 중 Chrome 포커스가 다른 프로그램으로 넘어가면 타이머를 자동 일시정지하고, 복귀 후 새 WH-C06 패킷을 다시 확인하도록 했다. 아브라행 완료 시 프로토콜, 목표 강도, 목표 하중, 최대 측정 하중, 하중달성률, 날짜/시간을 훈련 히스토리에 저장하고, 기존 완료 시각 로그는 측정누락 히스토리로 1회 마이그레이션한다. 히스토리 항목은 리스트에서 편집/삭제/전체 삭제할 수 있다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 포커스 이탈 대응에서 타이머 자동 일시정지를 제거했다. Chrome/Web Bluetooth 광고 스캔은 포커스 이탈 시 중단될 수 있으므로 타이머는 계속 진행하고, Chrome 복귀/페이지 표시/포커스 이벤트에서 WH-C06 광고 감시를 즉시 재확인하도록 변경했다. 안내 문구도 타이머 지속과 복귀 후 재감시 기준으로 수정했다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 수신이 멈춘 뒤 새로 스캔해도 페이지 새로고침 전까지 값이 돌아오지 않는 문제를 줄이기 위해 수동 WH-C06 새 스캔 경로를 강제 재시작 방식으로 바꿨다. 장치 선택 후 기존 광고 감시를 `unwatchAdvertisements()`로 끊고 짧은 간격 뒤 새 `watchAdvertisements()`를 시작하며, 수동 재스캔 후 20초 동안 새 패킷이 없으면 저울 전원 재시작 안내를 표시한다.
- `index.html`, `style.css`, `i18n.js`, `abrahang-timer.js`: 아브라행 훈련 히스토리를 훈련명과 날짜/시간 중심의 접이식 리스트로 바꿨다. 목표 강도와 목표 하중은 `목표 설정` 한 항목으로 묶어 중복 노출을 줄이고, 설정값 누락은 `설정누락`, 실제 측정값 누락은 `측정누락`으로 구분한다. 최대 측정 하중과 하중달성률은 항목을 클릭/터치했을 때만 상세로 표시된다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: 아브라행 저울 패널의 중복 `목표 하중` 칸을 `피크 하중`으로 바꾸고, 표준 저울/WH-C06 수신값에서 계산한 실제 하중의 최대값을 유지 표시하도록 했다. 피크 하중은 새 타이머 시작, 초기화, 새 저울 연결, 저울 방식/체중 변경 시 초기화된다.
- `index.html`, `abrahang-timer.js`: 아브라행 강도 목표 슬라이더 값을 브라우저 로컬 저장소에 보존해 새로고침 후에도 마지막 설정값이 유지되도록 했다. 저장값이 없을 때만 기존 Abrahangs 기본 50%를 적용하고, 모드 버튼을 새로 누르면 해당 모드 기본값을 다시 저장한다.
- `index.html`, `i18n.js`, `abrahang-timer.js`: WH-C06 수신이 작업 전환/스크린샷 직후 먹통이 되는 문제를 줄이기 위해 `blur`/`focus`/`visibilitychange`/`pagehide`를 WH-C06 복구 트리거로 쓰지 않도록 변경했다. 프리카운트 종료와 세트 전환 때 자동으로 `watchAdvertisements()`를 다시 호출하던 경로도 제거해, 작업 전환은 앱 내부에서 수신 오류나 재감시 상태를 만들지 않고 기존 광고 감시를 유지한다.
- `index.html`, `style.css`, `abrahang-timer.js`: 아브라행 훈련 히스토리를 오른쪽 설정 패널에서 왼쪽 타이머 패널 하단으로 옮기고, 리스트 요약을 `훈련명 / 하중(kg) / 날짜` 순서로 단순화했다. 레거시 기록도 `기존 로그` 접미사를 표시하지 않고, 목표 하중이 없으면 `설정누락`으로 표시한다.
- `index.html`, `abrahang-timer.js`: 아브라행/Emil 타이머에서 마지막 로딩 세트 뒤에는 휴식 타이머로 넘어가지 않고 즉시 완료되도록 총 시간 계산과 단계 전환을 수정했다. 완료 알람은 긴 비프 3회 패턴으로 바꿨다.
- `lottery-generator.js`: 로또번호 생성기를 1~10, 11~20, 21~30, 31~40, 41~45 구간 프로파일 기반의 디퓨전식 생성 흐름으로 확장했다. 과거 회차의 구간 패턴/전이/최근 흐름/쌍 조합 점수를 바탕으로 노이즈 번호를 12단계 복원하고, 최종 5세트를 선별한다.
- `index.html`, `style.css`, `i18n.js`: 로또 알고리즘 패널에 `구간 디퓨전` 프로파일 표시와 복원 단계 표시를 추가하고, 생성하기 연출이 노이즈 복원 진행률을 보여주도록 문구와 스타일을 갱신했다.
- `lottery-generator.js`, `style.css`, `i18n.js`, `index.html`: 디퓨전 연출이 6개 번호만 빠르게 깜빡이지 않도록 바꾸고, 45개 전체 후보 풀이 천천히 줄어든 뒤 살아남은 번호들이 A~E 5개 세트 슬롯으로 배분되는 흐름으로 개선했다.
- `assets/images/chakra-body-outline.png`, `index.html`, `style.css`: 이미지 생성으로 만든 명상 인체 외곽선 자산을 차크라 선택기 배경에 배치하고, 1~7번 차크라 버튼을 인체 중심선의 위치에 맞춰 표시하도록 조정했다.
- `assets/images/chakra-body-outline-right.png`, `index.html`, `style.css`: 차크라 선택기에서 버튼 축과 인체 외곽선 이미지 영역을 분리하고, 오른쪽 반신 전용 보정 이미지를 사용해 실루엣이 보이도록 했다. 1~7번 버튼 간격도 넓혀 겹침을 해소했다.
- `index.html`, `style.css`: 호흡타이머 차크라 선택기 실루엣 배경을 이전보다 더 크게 아래로 내리고 세로 크기를 유지해 7번 버튼이 머리 위, 1번 버튼이 하단/발 위치에 더 가깝게 오도록 조정했다.

## 2026-06-29
- `index.html`: 새 `lottery` 탭과 앱 가이드 글을 추가하고, 로또번호 생성기 스크립트/캐시 버전을 연결했다.
- `style.css`: 로또번호 생성기 결과 세트, 번호볼, 데이터 집계, 알고리즘 설명 패널의 반응형 스타일을 추가했다.
- `i18n.js`: 로또번호 생성기 탭/버튼/상태/가이드 문구를 한국어와 영어 번역 키로 추가했다.
- `lottery-generator.js`: 번들 이력, 브라우저 캐시, 공식 API, 공개 JSON 보조 소스를 조합해 로또 6/45 당첨번호 데이터를 갱신/저장하고 5개 추천 세트를 생성하도록 추가했다.
- `assets/data/lotto-645-history.json`: 앱 초기 로딩용 로또 6/45 역대 당첨번호 스냅샷을 추가했다.
- `index.html`, `style.css`, `i18n.js`, `lottery-generator.js`: 로또 탭 첫 진입 시 추천번호가 자동 노출되지 않게 바꾸고, `생성하기` 클릭 후 번호볼 뽑기 연출을 거쳐 결과가 표시되도록 수정했다. 타이틀 아래 중복 설명과 더보기의 데이터 로딩 설명을 줄이고 알고리즘 요약만 남겼다.

## 2026-07-29
- `AGENTS.md`, `RESEARCH_SPEC.md`, `DATA_SOURCES.md`, `STATE.md`: KOSPI·KODEX 일일 리서치의 상시 규칙, 검증 범위, 출처 우선순위, 실행 상태 보존 규칙을 추가했다.
- `.agents/skills/kospi-daily-research/SKILL.md`, `.agents/skills/kospi-daily-research/agents/openai.yaml`: 최신 시각·시장 상태 확인부터 메모리 수급, 미국 반도체, 경제 캘린더, 환율·수급, 시나리오 작성까지 재사용 가능한 로컬 스킬로 구성했다.
- `scripts/update_yield_spreads.py`: FRED의 10Y-2Y·10Y-3M과 3개월·2년·10년 절대금리를 내려받아 2년 장기 추세와 최근 90일 확대 차트를 생성하도록 추가했다. 장기 차트의 일별 점을 제거하고 최근 차트의 최신 라벨을 우측 여백에 분리했다.
- `reports/2026-07-29.md`, `charts/us_yield_spreads_long_term_2026-07-29.png`, `charts/us_yield_spreads_90d_2026-07-29.png`: 2026년 7월 29일 10:02 KST 기준 첫 실제 리서치와 검증된 금리차 차트 2개를 생성했다.
- `reports/2026-07-29-1037.md`, `STATE.md`: 10시 37분 장중값으로 보고서를 갱신하고, 10시 02분 전망의 하단 실패와 새 확인선을 기록했다.
- `reports/2026-07-29-1048.md`, `STATE.md`: 게시 직전 급락을 반영해 10시 48분 속보 보고서와 확인선을 추가 갱신했다.
- `articles/market-2026-07-29.html`, `articles/index.html`, `sitemap.xml`: 외국인·기관·프로그램 순매수에도 KOSPI가 6,000선을 이탈한 오전장을 SK하이닉스 실적, 메모리 수급, 환율, 미 국채와 함께 정리한 시황 아티클을 발행 목록과 사이트맵에 추가했다.
- `amplify.yml`: 리서치 아티클이 참조하는 `charts/*.png`를 Amplify 배포 산출물에 포함해 라이브 금리차 차트 404를 복구했다.
- `assets/images/articles/market-2026-07-29-support-break-1200x630.webp`, `articles/market-2026-07-29.html`, `articles/index.html`, `sitemap.xml`: 7월 29일 시황 글에 기존 아티클과 같은 어두운 3D 오브젝트 계열의 생성형 대표 이미지를 추가하고, 목록·Open Graph·Twitter·구조화 데이터·이미지 사이트맵을 새 썸네일로 통일했다.
- `style.css`, `articles/index.html`: DOM 첫 카드에만 420px 이미지 열을 강제하던 `:first-child` 예외를 제거해 카테고리 필터와 관계없이 모든 아티클 썸네일을 동일한 최대 300px 열로 표시하고, 스타일 캐시 버전을 갱신했다.
- `articles/market-2026-07-29.html`: 금리차 차트의 제작 방식과 내부 보고서 기준을 설명하던 문장을 독자 관점으로 고쳐, 최신 금리차 수치·장기 추세·오전 전망과 실제 흐름의 차이를 바로 이해할 수 있게 정리했다.
## 2026-07-29
- `articles/ai-circular-deals-last-customer.html`: 엔비디아·클라우드·AI 모델 기업의 상호 투자와 장기 용량 계약을 ‘최종 유료 수요가 인프라 비용을 감당하는가’라는 관점으로 읽는 에세이를 추가했다. 최대 투자 의향·용량 약정·실제 출자·서비스 구매를 분리해 설명하고, NVIDIA·CoreWeave·Anthropic의 공식 발표와 SEC 공시를 자료 주석으로 연결했다.
- `assets/images/articles/ai-circular-deals-last-customer-1730x909.png`: 원본 캡처를 재사용하지 않고, 데이터센터와 열린 자본 흐름을 표현한 자체 생성형 대표 이미지를 추가했다.
- `articles/ai-circular-deals-last-customer.html`, `style.css`: 회사별 거래 금액을 모사하지 않고 자본 조달·용량 확보·수요 검증을 단계별로 구분한 반응형 HTML 도식으로 다시 구성했다. 화살표와 라벨이 카드에 겹치던 SVG 및 내부 가로 스크롤을 제거하고, 1열 모바일 구조에서도 텍스트와 관계가 온전히 읽히도록 했다. 본문은 독자가 `지도에서 생긴 질문 → 세 장부 → 발표 숫자의 성격 → 마지막 결제 → 확인 질문` 순서로 논지를 따라가도록 전면 퇴고하고 반복 문장과 설명 없이 이어지던 숫자를 정리했다.
- `articles/index.html`, `sitemap.xml`, `amplify.yml`: 새 에세이를 아티클 허브·구조화 데이터·이미지 사이트맵에 넣고, 생성형 PNG 대표 이미지가 Amplify 산출물에 포함되도록 구성했다.
# 2026-07-30
- `scripts/update_yield_spreads.py`, `charts/us_yield_spreads_long_term_2026-07-30.png`, `charts/us_yield_spreads_90d_2026-07-30.png`: 금리차 차트의 제작 설명을 독자가 금리·경기 의미를 이해할 수 있는 한글 문장으로 교체하고, 맑은 고딕을 우선 적용해 한글 렌더링을 보장했다. 2년 추세와 최근 90일 차트를 FRED 최신 확정치(10Y-2Y +0.45%p, 10Y-3M +0.84%p)로 갱신했다.
- `reports/2026-07-30.md`, `STATE.md`: 09:11 KST의 KOSPI·반도체·KODEX 장중값, 미국 7월 29일 정규장과 시간외 반응, FOMC·Microsoft 공식 발표, 메모리 현물가격, 삼성전자 실적 콜과 미국 GDP·PCE 일정을 분리해 저장했다.
- `articles/market-2026-07-30.html`, `articles/index.html`, `sitemap.xml`, `assets/images/articles/market-2026-07-30-ai-demand-rate-tension-1200x630.webp`: 독자용 장중 시황 글과 통일된 1200×630 WebP 대표 이미지를 추가하고, 아티클 허브·구조화 데이터·이미지 사이트맵을 최신 발행일 기준으로 갱신했다. 글에는 고정 기준시각과 이후 변동 가능성을 명시하고, 차트 제작 과정이 아닌 수치가 뜻하는 시장 상황만 설명했다.
# 2026-07-31
- `reports/2026-07-31.md`, `STATE.md`: 07:25 KST 개장 전 기준으로 7월 30일 한국장 종가·외국인 및 프로그램 수급·시장 폭, 미국 반도체 정규장과 Amazon·Apple 시간외, 최신 DRAM 현물·삼성전자·SK하이닉스 실적, 달러/원, 미 국채와 당일 경제 일정을 새로 검증해 보존했다.
- `charts/us_yield_spreads_long_term_2026-07-31.png`, `charts/us_yield_spreads_90d_2026-07-31.png`: FRED 7월 30일 최신 일별값을 반영해 10Y-2Y +0.45%p, 10Y-3M +0.86%p 차트를 갱신했다.
- `articles/market-2026-07-31.html`, `articles/index.html`, `sitemap.xml`: AWS 37% 성장과 미국 반도체 반등, 전일 KOSPI의 지수·시장 폭 괴리, 높은 10년물 금리를 함께 읽는 독자용 개장 전 시황을 추가했다. 아티클 허브·ItemList·카테고리 수·이미지 사이트맵을 발행일 순서로 동기화하고, 본문 분량 기반 읽기 시간은 표시하지 않았다.
- `assets/images/articles/market-2026-07-31-ai-capex-rate-pressure-1200x630.webp`: 기존 시황 게시물과 같은 어두운 3D 스틸라이프 톤으로 AI 메모리 수요와 금리 압력을 표현한 글자·로고 없는 1200×630 WebP 대표 이미지를 생성했다.
- `style.css`, `articles/index.html`, `articles/market-2026-07-31.html`: 첫 번째 게시글 카드에만 적용되던 추가 패딩을 제거하고 스타일 캐시 버전을 갱신해 데스크톱과 모바일 모두 최신 글과 기존 글의 대표 이미지 표시 폭을 동일하게 맞췄다.
- `.agents/skills/kospi-daily-research/SKILL.md`, `.agents/skills/kospi-daily-research/references/issue-radar.md`, `RESEARCH_SPEC.md`, `DATA_SOURCES.md`: 고정 지표 수집 전에 가격 이상·공시·개방형 헤드라인을 탐색하는 밤사이 이슈 레이더를 추가했다. 후보 중요도 순위, 가격 이상 문턱, 출처 등급, 시차 중복 선제 제외, 청산·계약의 사실 경계, 장중 20분·정규판 30분 발행 상한을 규칙으로 고정하고 모의 이슈로 포워드 테스트했다.
- `.agents/skills/kospi-daily-research/references/thumbnail-direction.md`, `docs/CONTENT_GUIDELINES.md`: 이전 썸네일을 생성 참조로 사용하지 않고 당일 주도 사건·방향·정서·색과 빛에서 새 콘셉트를 만들도록 했다. 약세장에도 검정·차콜·붉은 하락 이미지를 반복하지 않고 제작 메모를 독자 본문에서 제외하는 기준을 추가했다.
- `reports/2026-07-31-0810.md`, `STATE.md`: Situational Awareness 공개주식 북의 Citadel 매각과 삼성전자의 데이터센터 고객 5곳 장기 메모리 계약을 08:10 KST 정정판의 최상위 사건으로 기록했다. 펀드 전면 청산·파산, 고객명·계약금액·기간처럼 확인되지 않은 범위는 분리했다.
- `articles/market-2026-07-31.html`, `articles/index.html`, `sitemap.xml`: 7월 31일 글의 제목·도입부·구조화 데이터·FAQ를 두 핵심 사건 중심으로 교정하고, 기존 AWS·미국 반도체·금리 분석은 전달 경로를 설명하는 보조 근거로 재배치했다.
- `assets/images/articles/market-2026-07-31-portfolio-transfer-memory-bottleneck-1200x630.webp`: 기존 썸네일을 참조하지 않고 밝은 아침빛 속 공개주식 포트폴리오 이동과 메모리 공급 병목을 표현한 글자·로고 없는 1200×630 WebP 대표 이미지를 새로 생성했다.
- `analytics.js`, `analytics.css`, 공개 HTML 26개: 기존 GA4 웹 스트림 `G-CCPBB462DH`를 전 페이지에 연결했다. 사용자가 통계를 허용하기 전에는 Google 태그를 불러오지 않는 동의 배너와 하단 통계 설정 버튼을 추가하고, 광고 저장·개인 맞춤 신호는 항상 거부하도록 구성했다.
- `analytics.js`, `app.html`: 도구 탭 전환 이벤트를 `tool_view`와 가상 `page_view`로 기록해 호흡·CO2·암벽화 핏·시퀀스·로또·아브라행 사용량을 분리 집계하도록 했다. GA4에는 이벤트 매개변수 `tool_name`을 보고서에서 쓸 수 있도록 맞춤 측정기준 `도구 이름`으로 등록했다.
- `privacy.html`: Google Analytics에서 처리할 수 있는 방문 정보, 전송하지 않는 도구 입력·기록, 동의 저장과 철회 방법을 개인정보처리방침에 명시하고 최종 업데이트 날짜를 갱신했다.

# 2026-08-01
- `articles/ai-leverage-position-exposure.html`: Situational Awareness의 상장주식 포트폴리오 매각을 계기로 레버리지·정보 비대칭·시간 제약을 읽는 에세이 `늑대들은 포지션의 냄새를 맡는다`를 추가했다. 매체별 매각 범위 차이와 확인되지 않은 마진콜 조건을 본문에서 구분하고, 특정 금융회사의 불법행위나 반도체 급등락의 단일 인과로 확대하지 않았다.
- `assets/images/articles/ai-leverage-position-exposure-1200x630.webp`: 반도체 칩이 든 모래시계와 주변의 늑대 모양 그림자로 포지션과 시간표의 노출을 표현한 글자·로고 없는 1200×630 대표 이미지를 새로 생성했다.
- `articles/ai-leverage-position-exposure.html`, `style.css`: 대중·프라임브로커·대량 포트폴리오 매수자·2차 시장 투자자가 서로 다른 시점의 정보를 보는 구조를 2열·모바일 1열의 반응형 HTML 개념도로 구성했다.
- `articles/index.html`, `sitemap.xml`, `articles/market-2026-07-31.html`: 새 에세이를 아티클 허브 최상단과 ItemList·이미지 사이트맵에 추가하고 전체 10편·에세이 3편으로 동기화했다. 기존 7월 31일 시황에서 새 에세이로 이어지는 내부 링크도 추가했다.
- 검증: 신규 글·허브의 JSON-LD와 사이트맵 XML, 로컬 링크·fragment·중복 ID, 1200×630 WebP 규격을 자동 검사했다. 로컬 브라우저의 1265px 데스크톱과 390px 모바일에서 대표 이미지·목차·반응형 개념도·카테고리 필터를 확인했으며 가로 넘침과 콘솔 오류가 없었다.
- `articles/ai-leverage-position-exposure.html`, `style.css`: 본문 공통 문단색이 흰 카드 설명을 덮던 CSS 우선순위 충돌을 바로잡고 카드·라벨·결론·캡션의 대비와 글자 크기를 높였다. 개념도 설명은 `투자 주체별 정보 시차 개념도.` 한 문장만 남겼으며, 상단 면책문과 반복적인 방어 표현을 걷어냈다.
