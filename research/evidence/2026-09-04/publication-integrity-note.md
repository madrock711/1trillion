# 9월 4일 발행·평가 검증 기록

- 08:18:19 KST: 최종 데이터 확인·원고 작성·전망 봉인.
- 08:19:30 KST: `b3557ced4b8387c6f08a38c14a9443397f97dc25` main push.
- 08:20:31.703 KST: Amplify 562 SUCCEED. 기사 최초 공개 가능 시각.
- 08:21:20 KST: custom·Amplify 두 도메인 cache-busted 20개 파일이 실제 배포 커밋과 SHA-256 일치. 로컬 작업 트리는 rebase 과정에서 CRLF로 체크아웃되어 커밋과 원시 바이트가 달랐으나 내용은 동일했다.

## 누적 평가 검증 실패

`2026-09-04-0818-same-close`의 보고서 해시는 Python Windows 기본 텍스트 쓰기로 생성된 CRLF 바이트를 대상으로 계산됐다. Git은 커밋 때 LF로 정규화했다.

- 봉인된 CRLF 해시: `0e30d0276fb1ba06c072f6bd21f0a3941bfa8be1c31ad846578678b942fd4b8e`
- 실제 push 커밋의 LF 보고서 해시: `5a018057e59eff77b0a4cc327b14b9bb0aadde9b5aac323b8f7196ba5fc9d9d2`
- 검증 오류: `pushed commit report differs from reportSha256`
- 본문 값과 문장은 동일하지만 원시 바이트 봉인 계약을 충족하지 못했다. 기존 전망·발행 이벤트를 사후 수정하거나 검증기를 완화하지 않았다. 오늘 전망을 유효한 누적 평가 표본이라고 보고하지 않는다.
- `generated/latest.*`는 새 전망을 추가하기 전의 9월 3일 정산까지 성공한 요약을 보존한다. 전체 원장 재실행은 위 오류로 중단된다. 따라서 오늘 실행의 전체 검증 완료 조건은 미충족이다.

## 다음 작업

1. 향후 보고서는 `write_bytes(text.encode('utf-8'))` 또는 `newline='\n'`로 LF를 명시하고, `git show :reports/...`의 스테이징 바이트와 해시를 대조한 뒤 봉인한다.
2. 실패 판본을 원문 그대로 보존하면서 누적 평가에서 명시적으로 무효 처리할 정정 절차가 필요하다. 기존 공개 판본의 해시·시각을 덮어쓰는 방식은 사용하지 않는다.
3. 작업 도중 원격에 추가된 `6d4817c`의 9월 3일 KOSPI·KODEX 분봉 아카이브는 rebase로 보존했다. 최초 조사 뒤 확보된 자료이므로 다음 정산에서 15:20 관측 복구 여부를 확인한다.

## 시각·회귀 확인

- 작성 시각 08:18과 데이터 기준 08:18:19를 기사에서 확인했다. 이 기록의 08:20:31.703이 실제 최초 배포 완료 시각이다.
- 신규 썸네일 1200×630, 데스크톱·390px 가로 넘침 0, 최신 목록 연결을 확인했다.
- Node 게시·대시보드·장전 회귀 5개, Python 평가기 단위 테스트 49개, diff check, pre-commit, 작업일지 가드는 통과했다. 누적 원장 검증 실패는 이 단위 테스트 통과와 별개다.
