# Agent Instructions

## 저장소 공통 규칙

- 새 세션 시작 시 `ProjectInfo.MD`의 최신 세션 요약을 1회 확인한다.
- 작업 시작 전에 `git rebase origin/main`을 수행한다.
- 문서는 기본적으로 한글로 작성한다.
- 코드나 설정을 수정하면 `docs/현재작업중.md`에 변경 파일과 작업 내용을 갱신한다.
- 커밋 전 `.githooks/pre-commit`과 `scripts/check_worklog_guard.sh`를 통과시킨다.
- 작업 완료 후 관련 변경만 커밋하고 현재 브랜치를 원격에 푸시한다.
- `docs/screenshot/`의 이미지 변경은 작업 중 발생 가능한 예외로 보고, 이번 작업과 무관하면 건드리지 않는다.

## KOSPI·KODEX 리서치

- “오늘의 리서치”, “KOSPI 전망”, “KODEX 리서치”, “메모리 수급 리서치” 요청에는 `.agents/skills/kospi-daily-research/SKILL.md`를 사용한다.
- 실행할 때마다 `RESEARCH_SPEC.md`, `DATA_SOURCES.md`, `STATE.md`를 먼저 읽는다.
- 이전 보고서의 숫자를 최신값처럼 재사용하지 않는다. 모든 시세·일정·뉴스는 실행 시점에 새로 검증한다.
- 정규장 종가, 시간외, 프리마켓, 장중값을 구분하고 기준 시각과 시장 상태를 함께 기록한다.
- 공식·1차 자료를 우선하고, 시장 시세와 해설은 신뢰할 수 있는 보조 출처로 교차 확인한다.
- 확정 사실, 시장 해석, 조건부 추론을 문장 수준에서 구분한다.
- 미국 ETF나 ADR이 한국장의 이미 끝난 움직임을 후행 반영했으면 같은 충격을 다음 한국장 전망에 중복 적용하지 않는다.
- 메모리 펀더멘털과 주식 수급·밸류에이션을 분리해서 평가한다.
- KODEX 레버리지의 일반적 배수 구조는 반복 설명하지 않는다.
- 결과는 `reports/YYYY-MM-DD.md`, 차트는 `charts/`에 보존하고 기존 보고서를 덮어쓰지 않는다.
- `STATE.md`에는 최신 기준일, 직전 종가, 핵심 수급, 다음 이벤트, 직전 전망의 적중·실패 요인을 짧게 남긴다.
